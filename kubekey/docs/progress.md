# ani-installer progress

- 2026-09-25, R16 closeout — sign-off for the selected combination only (pushed to origin/main on explicit user instruction): R16 live acceptance PASSED for exactly one matrix row — Ubuntu 24.04 amd64, kcn network, Ceph storage, site `site-a-r2` (cert-manager, PostgreSQL, NATS, metrics, Loki, Fluent Bit; Valkey off; effective config digest `67969228…`; run `ani-ani-lab-20260925-120811`): clean ESXi-snapshot restore + three-node offline isolation (0 public-URL fetches), first install completed in 1540 s, smoke 606 s all six components pass, persistence proven with exactly one planned Pod recreation each for PostgreSQL and NATS (real data re-read, same PVC bindings). T-R16-02 planned Valkey addition passed with base invariance proven (namespace UIDs identical, original six connections.d fragments and run.json SHA unchanged; addition-target config digest `003b0da3…`); T-R16-03 bounded duplicate-plan refusal passed with zero writes; T-R16-04: Loki live pass, OpenSearch local pass only. Materials: an approver-lock mismatch caught at attempt r1 (postgres tag drifted copy `91eb910c…` ≠ approved `7bade6d5…`) was fixed in a materials round by re-staging the approved digest byte-exactly via skopeo dir transport (hauler v1 digest-source rewrite defect recorded); rebuilt haul `a9017e42…`, artifact `ani-artifact-ubuntu24-amd64-r16fixed-20260925-132455`. NOT verified and NOT signed off (the rest of the R-card campaign also stays where `../docs/execution/progress.yaml` marks it — R04's GitHub CI got its first real run from this push: red on run 1 (a `git show HEAD:` control-test time bomb surfaced by landing the very fixes the controls assumed were still only local), fixed by pinning the three control fixtures to the blueprint commit and `fetch-depth: 0`, green on run 2; R06–R14 live_pending items remain open): kubeovn full chain, external storage class live, cold-registry start, podCIDR/quoted-YAML variants live, OpenSearch live. Credentials: user standing decision — no rotation and no forced storage refactor this round; this push additionally removes the hardcoded ESXi default from `restore_esxi_snapshots.sh` (now `ESXI_PASS`/`ESXI_PASS_FILE` only, 0600 file kept outside the repo); an earlier misdiagnosed "ESXi lockout" was closed as a wrong-credential-source rejection, legitimate every time, resolved after the user personally verified the correct dedicated credential (no value appears in any repo document). A node password literal remains in the already-published plan document — registered as R01 manual follow-up. Out-of-scope defects registered untouched: defect 8 (render heredoc exemption misses metrics verify), run-state phase stays `registry_content_verified` on success, `sourceTreeFingerprint == configDigest`, `--kk` has no PATH fallback, v2b podCIDR overlap not rejected offline. Nine-stage timings, support matrix and the full handoff are published under `../docs/execution/evidence/R16-20260925/` (`task-result-r2-final.yaml`, `r16-timing-table.md`, `r16-support-matrix.md`), with raw private evidence retained outside the repository under `~/workspace/ani-installer-work/evidence/R16-20260925/`. The pass sign-off applies only to the selected combination, not to any other version/network/config row.

- 2026-09-24, R00 (no build/install/source transfer): froze the authoritative work tree at HEAD `275827f17abeb0d84694d078d0bc2877307cf87e`, treeSHA256 `8c66e3a591ea3bbe2146fafde5713966cc24b24ba6283b6f1b0370eb8dd22410` (clean `git status`). Adopted the outer execution plan package in `../docs/execution/`; status in `../docs/execution/progress.yaml`, rulings in `../docs/execution/status-decision.md`, private evidence and `lab.env` kept outside the repo under `/home/chabking/workspace/ani-installer-work/`. Blocked: `ssh fedora` returns permission denied (exit 255) while this host's hostname is already `fedora`, so remote checks were replaced by local read-only probes and must not be reported as remote verification. Confirmed the kcn fix2 archive at `/tmp/kc-networking.tar` still matches its recorded sha256; its manifest digest and any live install remain unverified. HAProxy/kube-vip remain only as unused image-manifest entries because ANI sets `control_plane_endpoint.type=local`. No product code, lock file, image digest, component version, snapshot or target host was touched; nothing committed or pushed.

- 2026-09-19, live observability review: user authorized existing kcn material with optional controller restart. Built unmodified c7661ca code (kk SHA256 e986c0eadadcfeade6870f5d40823b82435ce13679aa7d4ce259aed14d99b914) and a separate cumulative artifact on Fedora. Two clean-snapshot offline installs on .20/.21/.22 failed: cumulative Loki configuration at metrics namespace creation (wrong `.ani.metrics.namespace` context; 470/459/10/1), and metrics-disabled Loki configuration at `/config` JSON parsing (actual response is YAML; 495/484/10/1). Loki Ready/buildinfo/empty query passed, but Fluent Bit was not reached. OpenSearch was not run. Both failures belong to installer; no manual kcn-controller restart and no product fix performed. Paused after the second failure at the user's request, preserved the cluster/evidence and released the task's flock. See outer `docs/observability-live-handoff-20260919.md`; historical C0-C5 code-pass claims below are superseded by the current status document. No commit/push.

- 2026-09-18: Fixed the manual-entry kubeconfig defect. ANI's KubeKey child explicitly uses `/etc/kubernetes/admin.conf`; Init/Join exports use the configured connector user and getent home/UID/GID instead of SUDO_USER, with 0700 directory and 0600 config permissions. Fedora sandbox export tests plus ANI/connector and builtin tests passed. Built independent `fix-kubeconfig-20260918/ani-code-kubeconfig-r12`, kk SHA256 `288574d20507a58c3431b9fbb8d0fa41bc73493663092af080020ce1caa84dbd`; release checksum/help checks passed, unchanged r2 artifact reused. No live repair, reset, install, target binary replacement, or push performed. Clean-snapshot installation through the non-root entry remains not_verified. See outer `docs/kubeconfig-fix-20260918.md`.

- 2026-09-18 10:14–10:25 (Asia/Shanghai): After explicit user authorization, repaired the Debian repository task to preserve APT failures, use isolated ISO sources/lists/cache, and install noninteractively with bounded dpkg lock waiting. Execution-level regression reproduced old update failure being swallowed (100 -> 0); all 3 regression branches and selected Go/builtin tests passed on Fedora. Built separate code `fix-apt-20260918/ani-code-aptfix-r11`, kk SHA256 `18cf5ae6905649afd1017c040f8a77633cf835114670244a28b7463a24010007`, reusing unchanged r2 artifact. Restored only .20–.22 snapshots, verified clean disks and offline isolation, and completed one fresh install 10:14:32–10:24:14: exit 0, 413 total / 403 success / 10 ignored / 0 failed, including Ceph RBD/CephFS write/read verification. Independent verify exit 0, 28 images, 3 Ready nodes, network/Envoy probes passed. All 3 nodes have installed chrony and active service. Ceph remains HEALTH_WARN (AES authentication and PG count), 3 OSD up/in and 265 PG active+clean; no component workaround applied. S3 API and HA/power-loss tests not run. Evidence: `fedora:/home/chabking/ani-installer-runs/fix-apt-20260918/`; outer report `docs/offline-apt-fix-validation-20260918.md`. No push performed.

- 2026-09-18 09:40–09:44 (Asia/Shanghai): User-authorized clean-snapshot recheck of the existing r10 code release + r2 artifact FAILED. Through `ssh fedora`, restored only 172.16.101.20–22 (VMIDs 5/6/7, snapshot 1), confirmed clean nodes and blank sdb, applied existing offline isolation, and transferred checksum-verified separate releases. The sole install ran 09:43:23–09:44:42, exit 1, 136 total / 132 success / 3 ignored / 1 failed. Failure: `NTP | Configure NTP server` on node1, `/etc/chrony/chrony.conf: No such file or directory`; read-only follow-up confirms chrony is not installed. Kubernetes/Ceph deployment and independent verification were not reached. No code changes, rebuild, repair, or retry; failed scene retained. Evidence on Fedora: `/home/chabking/ani-installer-runs/recheck-20260918/`; report: `docs/clean-reinstall-recheck-20260918.md` in the outer workspace. This result applies to the existing r10 binary, not a fresh build of local source.

- 2026-09-15 10:55–10:57 (Asia/Shanghai): S1 code/build completed. Created local fork from upstream KubeKey tag v4.0.7 at commit 9b38d25d6514758afc97559dc6f111eda31a4e82, branch `main`, on Ubuntu 24.04 amd64 builder `Ubuntu-24`. Built `_output/bin/kk` with `PATH=/usr/local/go/bin:$PATH make kk` using Go 1.26.4. Verified `kk version` reports v4.0.7/9b38d25d6514758afc97559dc6f111eda31a4e82 and `kk create cluster --help` exposes the expected artifact/config/inventory/workdir options.
- First real offline install: not started (dedicated three-VM input not yet provided).
- Clean-snapshot reinstall: not started.

- 2026-09-15 12:20–12:22 (Asia/Shanghai): S2 code/package build completed. Built the relocatable package at `/home/ubuntu/workspace/ani-installer-bundle/ani-offline-ubuntu24-amd64-20260915-s4`. Package validation passed: `sha256sum -c SHA256SUMS`, 16 images loaded from `images/images.haul.tar.zst`, 16 manifests served from a fresh builder-local Hauler registry on port 5001, and packaged `kk ani install --help` succeeded. The kcn role was created from the exact v0.6.2 manifest replacement asset (SHA256 `D1C69F5D980A0A81D5C71253DB51537AF08F9E369F88A0FECE25C1CAEDFA0E97`), preserving the original three-replica controller and three-replica OVN-central layout. First real offline install: not started; dedicated three-VM input is still missing, so target-node Hauler, containerd, and CRI pull have not yet been validated on the real VMs. Clean-snapshot reinstall: not started.
- 2026-09-15 12:27–12:33 (Asia/Shanghai): S2 package rebuilt with a corrected `verify.sh` that resolves the installer node address for registry checks. New relocatable package: `/home/ubuntu/workspace/ani-installer-bundle/ani-offline-ubuntu24-amd64-20260915-s2b`. Validation passed: `sha256sum -c SHA256SUMS`, packaged `kk ani install --help`, `bash -n verify.sh`, and 16/16 image manifests served from a fresh builder-local Hauler registry. This package supersedes the `s4` package for the first real install, and a fresh relocation check from a new hard-linked path passed. First real offline install: not started (dedicated three-VM input is still missing). Clean-snapshot reinstall: not started.
- 2026-09-15 12:33 (Asia/Shanghai): Corrected package README to run verification with `sudo`, rebuilt the `s2b` package, and revalidated `sha256sum -c SHA256SUMS`, packaged `kk ani install --help`, and README content.
- 2026-09-15 12:34–12:35 (Asia/Shanghai): Added config tests proving that installer-node order does not change the local connector, registry address, insecure-registry setting, or rendered image reference. Local `go test ./pkg/ani` and `go test -tags builtin ./pkg/ani` pass.- 2026-09-15 12:41–12:44 (Asia/Shanghai): S2 code/package build corrected for site-specific kcn values. Replaced the kcn template's hard-coded service CIDR and OVN/NODE IPs with `service_cidr` and `node_addresses` rendering, aligned the `ani.network.kcn` keys with `managedDevices`/`encapNetworks`/`intranetNetworks`, and added template/config tests. `go test ./pkg/ani`, `go test -tags=builtin ./pkg/ani`, and `go test -tags=builtin ./cmd/kk/app` pass. Rebuilt package `/home/ubuntu/workspace/ani-installer-bundle/ani-offline-ubuntu24-amd64-20260915-s2c`; `sha256sum -c SHA256SUMS`, packaged `kk ani install --help`, `bash -n verify.sh`, relocation from a hard-linked new path, and 16/16 manifest access through a fresh builder-local Hauler registry all pass; text materials contain no stale site values or build-machine paths. S2 real-target Hauler/containerd/CRI-pull checks and all real install stages remain blocked on the dedicated three-VM inputs; no first offline install or clean-snapshot reinstall has run.

- 2026-09-15 12:55–13:02 (Asia/Shanghai): S2 code/package build completed for password authentication and the corrected package. `SSHConfig` now accepts either `ssh.password` or `ssh.privateKey`; remote inventory connectors use the password form when supplied, and local installer execution remains local. Remote `go test ./pkg/ani`, `go test -tags=builtin ./pkg/ani`, and `go test -tags=builtin ./cmd/kk/app` pass. Rebuilt relocatable package `/home/ubuntu/workspace/ani-installer-bundle/ani-offline-ubuntu24-amd64-20260915-s2d` (about 1.3 GiB) with the verified Hauler store and fixed KubeKey artifact. Package checks passed: SHA256SUMS, `kk ani install --help`, `bash -n verify.sh`, hard-link relocation, and 16/16 image manifests through a fresh builder-local Hauler registry. Site facts recorded: installer node defaults to `node-20` (`172.16.101.20`); management interface `ens34`; actual UP business interface is `ens36` on `192.168.100.0/24` (`ens35` is DOWN with no address); defaults are Pod `10.16.0.0/16`, Service `10.96.0.0/16`, encap `192.168.100.0/24`, intranet `192.168.100.0/24` + Service CIDR, registry port 5000. First real offline install, real-target Hauler/containerd/CRI pull, and clean-snapshot reinstall remain not started pending the user restoring the three VMs from the clean snapshot.

- 2026-09-15 14:05 (Asia/Shanghai): Site correction and clean-target state recorded. User confirmed `ens36`/`192.168.100.0/24` is storage networking and is not used by kcn; kcn must take over the host-IP-free `ens35`. The three VMs were restored to the post-ISO clean snapshot. Actual hosts are `test-installer-01` (`172.16.101.20`), `test-installer-02` (`172.16.101.21`), and `test-installer-03` (`172.16.101.22`). Local site config was updated to `ens35`, default encap/business network `192.168.101.0/24`, Pod `10.16.0.0/16`, Service `10.96.0.0/16`, intranet `192.168.101.0/24` + Service CIDR, and actual hostnames; `ens36` is left untouched. First real offline install and clean-snapshot reinstall remain not started until package upload, offline enforcement, and target-node validation are executed.

- 2026-09-15 14:15 (Asia/Shanghai): S3 acceptance coverage was added without changing the component scope. The smoke role now creates a backend Service and runs a third-node client that separately verifies cross-node Pod-IP access, Service ClusterIP access, and cluster DNS. `verify.sh` requires the network client to report `ANI-NETWORK-OK` while retaining the Envoy HTTP `ANI-INSTALLER-OK` check. YAML structural validation and `go test ./pkg/ani`, `go test -tags=builtin ./pkg/ani`, and `go test -tags=builtin ./cmd/kk/app` pass. These updates are source-complete and will be applied to the uploaded package before any install attempt.

- 2026-09-15 14:25 (Asia/Shanghai): S2 package is now present on the real installer VM at `/home/ubuntu/ani-installer/ani-offline-ubuntu24-amd64-20260915-s2d`, with the S3 smoke-role patch included. Remote target validation passed: 28 package files, `sha256sum -c SHA256SUMS` all OK, `bash -n verify.sh` OK, and required scripts/binaries are executable. The site config was uploaded to `/home/ubuntu/ani-installer/cluster.yaml` with mode 0600. This validates package transfer/integrity on the real target, but Hauler/containerd/CRI-pull and first install remain pending until offline enforcement is applied.

- 2026-09-15 14:39 (Asia/Shanghai): Offline enforcement is active on all three nodes without removing the management default route. Runtime firewall chains allow loopback, the workstation SSH peer, management network 172.16.101.0/24, kcn business/encap network 192.168.101.0/24, Pod CIDR, and Service CIDR; unrelated IPv4/IPv6 traffic is dropped. Real-node tests passed on all three: SSH remains reachable, peer management TCP/22 works, ns35 is UP with no host address, ns36 is untouched/DOWN, and TCP/443 to 1.1.1.1 is blocked. An earlier direct default-route removal on the installer briefly blocked workstation SSH; it was immediately recovered through the in-network jump host, then replaced by this firewall-based offline enforcement.
- 2026-09-15 13:57:25 (+08:00): First real offline install started on the clean three-VM target using /home/ubuntu/ani-installer/ani-offline-ubuntu24-amd64-20260915-s2d/install.sh and /home/ubuntu/ani-installer/cluster.yaml. Log: /home/ubuntu/ani-installer/ani-offline-ubuntu24-amd64-20260915-s2d/logs/ani-lab/install.log.
- 2026-09-15 13:57–13:58 (Asia/Shanghai): First real offline install failed in KubeKey's containerd/runc task. The real log showed the runtime requested `.../runc/v1.2.6/amd64/runc.amd64`, while the package artifact contains `runc/v1.1.12/amd64/runc.amd64`. Target Hauler itself passed: the service started on the installer and all 16/16 image manifests were reachable. Source fix completed by pinning generated `cri.runc_version` to `v1.1.12` and updating `ani/package.yaml`; local Go tests passed.
- 2026-09-15 14:04–14:05 (Asia/Shanghai): A rebuilt `kk` with the runc pin passed remote package checksum/help/syntax checks, but the retry failed at native initialization with `/usr/bin/env: 'bash\r': No such file or directory`. Root cause was cross-building from a Windows checkout, which embedded CRLF shell assets. The corrected `kk` was rebuilt on the real Ubuntu 24.04 amd64 builder (`Ubuntu-24`) with LF assets and `-tags builtin`; builder Go tests and `make kk` passed. The corrected Linux binary and regenerated checksums were uploaded and revalidated on the real installer.
- 2026-09-15 14:14–14:21 (Asia/Shanghai): Fixed the real etcd failure in source by explicitly rendering internal etcd, 3.6.6, local registry kubernetes/etcd:v3.6.6; added config assertions. Builder go test ./pkg/ani, go test -tags=builtin ./pkg/ani, and go test -tags=builtin ./cmd/kk/app passed; rebuilt the Linux kk on Ubuntu, replaced only the package binary/checksum, and real-target package revalidation passed (sha256sum -c, help, syntax). Before the next retry, the partially installed attempt was removed with KubeKey's built-in delete cluster path; real delete log passed with 0 failed tasks. First real install remains not passed.
- 2026-09-15 14:38–14:43 (Asia/Shanghai): Corrected the real first-install failure in the ANI roles. The unsupported KubeKey ile: task was replaced with a POSIX command: install -d task in both kcn and smoke roles, and a regression test was added. After fixing the earlier bad builder sync and removing the accidental flat
oles/ani/main.yaml, Ubuntu-builder tests passed: go test ./pkg/ani, go test -tags=builtin ./pkg/ani, and go test -tags=builtin ./cmd/kk/app; rebuilt kk SHA256 is ccd1393f92d483b7faba10c8c5f7f5593b7e768647599fb73ddf1919e1139bd. Package binary, checksums, and both task manifests were re-uploaded to the real installer. Remote package checks passed (sha256sum -c, kk ani install --help, ash -n verify.sh). The partially installed cluster was inspected first: 3 nodes had been created by KubeKey but were NotReady pending CNI; core kube-system control-plane pods ran. KubeKey's built-in delete path then passed (43 total, 40 success, 3 ignored, 0 failed), and stale work/ani-lab/kubekey state was removed. Code/package and delete/reset are complete; first real install still not passed because the prior run failed before ANI/kcn.
- 2026-09-15 14:43–14:56 (Asia/Shanghai): Retry 2 of the first real install failed while waiting for kcn-cni-ds. The real daemon log showed:
o matching network interface found for encap networks: [192.168.101.0/24]. This was a site-config error: the encap network must match a physical host network, and the nodes' physical reachable underlay is 172.16.101.0/24 on ns34; ns35 correctly remains the KCN-managed host-IP-free device. Local and remote site configs were corrected to ncapNetworks: [172.16.101.0/24], while managedDevices: [ens35] and the intranet networks remain unchanged. First real install still not passed; clean reinstall not started.

- 2026-09-15 16:20-16:44 (Asia/Shanghai): S2 corrected source/package completed and validated. Builder tests passed: gofmt, go test ./pkg/ani, go test -tags=builtin ./pkg/ani, go test -tags=builtin ./cmd/kk/app, py_compile, make kk, and bash -n. Built relocatable package ani-offline-ubuntu24-amd64-20260915-s2e. Builder package checks passed: SHA256SUMS, kk ani install --help, install/verify syntax, no Python bytecode in manifests, corrected OVN Service names and leader checker, Envoy orphan-LSP task order, no builder/site paths, and 16/16 image manifests served from the packaged archive. The package was transferred to the real installer and passed the same integrity/content checks. The prior partial diagnostic cluster was deleted only through KubeKey delete cluster; the real delete log finished with 43 total, 40 success, 3 ignored, 0 failed. First corrected real install has not run yet.

- 2026-09-15 16:45-16:49 (Asia/Shanghai): S2 first corrected real offline install passed. The relocated package `ani-offline-ubuntu24-amd64-20260915-s2e` installed with KubeKey result 393 total, 387 success, 6 ignored, 0 failed. Real verification passed: 3/3 nodes Ready, 16/16 registry image manifests, and Envoy HTTP returned `ANI-INSTALLER-OK`. Logs: `/home/ubuntu/ani-installer/ani-offline-ubuntu24-amd64-20260915-s2e/logs/ani-lab/install.retry-5.stdout` and `verify-s2.retry-5.stdout`. Clean-snapshot reinstall remains not started.
- 2026-09-15 16:48 (Asia/Shanghai): S3 first real network smoke passed. Evidence captured at `/home/ubuntu/ani-installer/ani-offline-ubuntu24-amd64-20260915-s2e/logs/ani-lab/s3-network-smoke.stdout`: all 3 nodes Ready, `ani-smoke-network-client` phase `Succeeded`, and logs returned `ANI-NETWORK-OK`.
- 2026-09-15 16:48 (Asia/Shanghai): S4 first real Envoy Gateway validation passed. Evidence captured at `/home/ubuntu/ani-installer/ani-offline-ubuntu24-amd64-20260915-s2e/logs/ani-lab/s4-envoy-gateway.stdout`: GatewayClass accepted, Gateway programmed, controller Deployment Available=True, Envoy data plane 2/2 ready, client phase `Succeeded`, and HTTP returned `ANI-INSTALLER-OK`. Clean-snapshot reinstall remains not started.
- 2026-09-15 16:49-16:51 (Asia/Shanghai): S5 real-target relocation passed. Copied the complete package to `/home/ubuntu/ani-installer/ani-offline-ubuntu24-amd64-20260915-s2e-relocated`, switched the active `ani-image-registry.service` to that root, and verified it from the relocated package: `SHA256SUMS`, `kk ani install --help`, `install.sh`/`verify.sh` syntax, no original-root references outside runtime logs, 16/16 local registry images, 3/3 Ready nodes, and Envoy HTTP `ANI-INSTALLER-OK`. Evidence: `/home/ubuntu/ani-installer/ani-offline-ubuntu24-amd64-20260915-s2e-relocated/logs/ani-lab/s5-relocation.retry-3.stdout`. The first two relocation-check retries failed only because the validation script matched the source-root prefix too loosely and then double-substituted the already-relocated systemd unit; retry 3 corrected those test-script issues and passed. Clean-snapshot reinstall remains not started.
- 2026-09-15 16:51 (Asia/Shanghai): S6 clean-snapshot precheck only. `172.16.101.20` still contains the first-install state: `/etc/kubernetes/admin.conf`, `/home/ubuntu/ani-installer`, active kubelet/containerd services, and installed cluster resources are present. The post-ISO snapshot has therefore not yet been restored for S6. No clean reinstall has been executed.
- 2026-09-15 16:51:53 (Asia/Shanghai): S6 remains blocked on the required clean snapshot restore. `172.16.101.20` still has `/etc/kubernetes/admin.conf`, `/home/ubuntu/ani-installer`, active kubelet/containerd, and the first-install cluster state. No clean reinstall has been run.
- 2026-09-15 17:15-17:20 (Asia/Shanghai): S6 clean-snapshot reinstall passed. The three VMs were first confirmed restored to the post-ISO snapshot (`admin.conf` and `ani-installer` absent, kubelet/containerd inactive, `ens34` UP with 172.16.101.20~22, `ens35` DOWN with no host IP, `ens36` untouched), then the corrected package and site config were uploaded and IPv4 offline enforcement was verified on all nodes. Package: `/home/ubuntu/ani-installer/ani-offline-ubuntu24-amd64-20260915-s2e`; config: `/home/ubuntu/ani-installer/cluster.yaml`; install window UTC `2026-09-15T09:15:32Z`–`2026-09-15T09:19:33Z`. KubeKey result: 393 total, 383 success, 10 ignored, 0 failed; process exit status 0. Package verification passed: cluster=ani-lab, registry=16 images, nodes=3, Envoy HTTP=`ANI-INSTALLER-OK`. S6 network smoke passed: 3/3 Ready, `ani-smoke-network-client` phase `Succeeded`, logs=`ANI-NETWORK-OK`. S6 Envoy Gateway validation passed: GatewayClass Accepted=True, Gateway Programmed=True, controller Deployment Available=True with 1/1 ready, Envoy data plane ready, client phase `Succeeded`, and HTTP=`ANI-INSTALLER-OK`. Logs: `logs/ani-lab/install-s6-clean.stdout`, `verify-s6-clean.stdout`, `s6-network-smoke.stdout`, and `s6-envoy-gateway.stdout` under `/home/ubuntu/ani-installer/ani-offline-ubuntu24-amd64-20260915-s2e/`. Overall S1–S6 are complete; clean-snapshot reinstall passed.
- 2026-09-15 18:16 (Asia/Shanghai): post-S6 installer local-connector fix completed. `KubeKeyInventory` now emits `user: ubuntu` for the installer's local connector so KubeKey's native kubeconfig export creates `/home/ubuntu/.kube/config`. Tests passed: `go test ./pkg/ani`, `go test -tags=builtin ./pkg/ani`, and `make kk` on Ubuntu 24.04 amd64. The rebuilt Linux `kk` and refreshed SHA256SUMS were applied to the builder, Windows relay package, and real installer package; all package hashes revalidated. The current installer node kubeconfig was created from the generated admin.conf. This connector fix has not yet been revalidated by another clean-snapshot reinstall.
- 2026-09-15 18:24 (Asia/Shanghai): Source-tree relocation completed. The complete fork now lives at D:\Workspace\ani-installer\kubekey as a nested Go module; local relocation checks `go test ./pkg/ani` and `go test -tags=builtin ./pkg/ani` pass. The old D:\Workspace\ani-installer-kubekey root retains only non-source `.tmp` transfer/build artifacts. This is a source-layout relocation only; it is not a new clean-snapshot reinstall, and no cluster state was changed.
- 2026-09-15 18:32 (Asia/Shanghai): Confirmed the corrected non-root kubeconfig path is usable by `verify.sh` without `sudo`: `KUBECONFIG_FILE=/home/ubuntu/.kube/config ./verify.sh /home/ubuntu/ani-installer/cluster.yaml` passed from the real installer package, reporting cluster=ani-lab, 16 registry images, 3/3 nodes, network=ANI-NETWORK-OK, and Envoy HTTP=ANI-INSTALLER-OK. This validation did not alter cluster state.

- 2026-09-15 19:38–19:43 (Asia/Shanghai): Validation closeout completed. Replaced the old fixed-name network/Envoy clients with a common active `probe.sh`, made all request failures and wrong responses fail verification, selected the Envoy Service only by all four Gateway ownership/managed-by/name labels plus port `9090`, and required fresh generated-name clients each run. Added shell/Go regressions, including the real cluster's fully qualified EndpointSlice name. On `fedora`, `go test ./pkg/ani` and `go test -tags=builtin ./pkg/ani` pass. Revised package: `/home/chabking/ani-installer/ani-offline-ubuntu24-amd64-20260915-s2f` and installer copy `/home/ubuntu/ani-installer/ani-offline-ubuntu24-amd64-20260915-s2f`; checksums, syntax, `kk version -s` (`v4.0.7-closeout`), and `kk ani install --help` pass. Two active runs on the existing three-node cluster passed with real requests and four distinct new UIDs; both selected Envoy Service `ani-smoke` (`10.96.129.239:9090`) rather than the backend Service. A read-only external curl unexpectedly succeeded, so those runs are not offline-isolation evidence; no network/firewall/routing change was made. Historical first install and clean-snapshot reinstall records remain preserved. New clean installation with revised code and clean-snapshot revalidation of the local-connector fix remain not verified. Details: `kubekey/docs/validation-closeout.md`.
- 2026-09-17: Minimal offline-first-install iteration reached the ready-to-run checkpoint. Code and artifact outputs are now split: the code release contains only kk, install.sh, `verify.sh`, probe.sh, and SHA256SUMS, while the artifact release contains only fixed dependencies, images, metadata, repository ISO, and licenses. Source tests on `fedora` passed for go test ./pkg/ani, go test -tags=builtin ./pkg/ani, and go test -tags=builtin ./cmd/kk/app. Code release: /home/chabking/ani-installer-runs/platform-20260917/run/ani-code-20260917-r2. Artifact release: /home/chabking/ani-installer-runs/platform-20260917/run/ani-artifact-ubuntu24-amd64-20260917-r1. Both release checksums pass; the artifact contains no code, scripts, or manifests, and its fixed materials match the established baseline. No target nodes were touched. The accidental append corruption in this entry was fixed, local/remote `git diff --check` plus release contents/checksums were reconfirmed on `fedora`, and the target `site/cluster.yaml` was prepared, staged on `fedora`, and passed the package `Validate` check. Clean offline install and real network/Envoy acceptance remain not_verified, pending user restoration of the three-VM pre-install snapshots.

- 2026-09-17 20:09 (Asia/Shanghai): The user confirmed all three pre-install snapshots were restored. The restored nodes passed the expected clean-state checks (hostnames, `ens34` management addresses, no `/etc/kubernetes`, no `/var/lib/ani-installer`, and inactive containerd/kubelet). The required offline precondition then failed: all of `172.16.101.20`, `.21`, and `.22` resolved `example.com` and returned HTTP 200 from `https://example.com` over the default `172.16.101.1` route. No target mutation, package transfer, install, or acceptance verification was attempted. Read-only evidence: `/home/chabking/ani-installer-runs/platform-20260917/logs/offline-precheck-20260917-200926.log` on `fedora`. Clean offline first-install and real network/Envoy acceptance remain `not_verified`; the current blocker is environment isolation, not an installer or component failure.
- 2026-09-17 20:51 (Asia/Shanghai): The r3 clean-snapshot attempt failed during native OS initialization because the embedded `init-os.sh` had CRLF bytes, producing `/usr/bin/env: bash\r: No such file or directory`. This is an installer release defect, not a Kubernetes/containerd/kcn/Envoy defect. The source file was normalized to LF, `git config core.autocrlf` was set to `false`, and `TestExecutableReleaseFilesUseLF` now requires LF shebangs and no CR bytes for the shipped `install.sh`, `verify.sh`, and `probe.sh` plus the embedded native `init-os.sh`. Source tests passed (`go test ./pkg/ani`, `go test -tags=builtin ./pkg/ani`, `go test -tags=builtin ./cmd/kk/app`). Code release `/home/chabking/ani-installer-runs/platform-20260917/run/ani-code-20260917-r4` was rebuilt; checksums, shell syntax, and `kk ani install --help` pass. The existing artifact release `/home/chabking/ani-installer-runs/platform-20260917/run/ani-artifact-ubuntu24-amd64-20260917-r1` was not rebuilt and its checksums still pass. Current target state is not eligible for install: `.20` contains r3 code, the r1 artifact, and `/var/lib/ani-installer/ani-lab` runtime data from the failed run; `.21` and `.22` have no installer directories. All three nodes have the two public-network blackhole routes, external DNS/HTTP fail, `/etc/kubernetes` is absent, and containerd/kubelet are inactive. Next required action is a user restoration of all three pre-install snapshots before r4 is transferred and installed.

- 2026-09-17 (Asia/Shanghai): Recovered the true r4-r6 history from `platform-20260917/logs` (this file had stopped at the r3 failure / r4 rebuild). r4 (`install-r4-20260917-212830.log`, 136 total / 132 success / 3 ignored / 1 failed, exit 1) failed during native OS initialization: a task read and edited `/etc/chrony/chrony.conf`, which does not exist on the target (`No such file or directory`, exit status 2). r5 first correctly refused to run on dirty state (`runtime root /var/lib/ani-installer/ani-lab already exists; restore the clean snapshot before a new install`), then after a snapshot restore (`install-r5-after-restore-20260917-214602.log`, 136 / 132 / 3 / 1) failed on the same chrony.conf defect. r6 (`install-r6-20260917-140357.log`, 271 total / 260 success / 10 ignored / 1 failed, exit 1) progressed past chrony but failed at `[InitKubernetes | Generate kubeadm join token]` with `route ip+net: no such network interface`.
- 2026-09-17 to 09-18 (Asia/Shanghai): r6 root cause confirmed and fixed. The offline enforcement then in use added interface-less blackhole routes (`blackhole 0.0.0.0/1` + `blackhole 128.0.0.0/1`); these have ifindex 0 and `0.0.0.0/1` also covers the cluster Service CIDR `10.96.0.0/16` and Pod CIDR `10.16.0.0/16`, so kubeadm's Go net route enumeration aborts. It was replaced with netfilter-based enforcement (a dedicated `ANI-OFFLINE` chain jumped-to from OUTPUT/FORWARD, RETURN for loopback / ESTABLISHED,RELATED / RFC1918 / link-local / management-peer CIDR, DROP the rest; IPv6 mirrored; INPUT and the default route are never touched), which keeps the kernel routing table clean and matches the firewall-style enforcement the 2026-09-15 clean install passed with. Persisted as the reusable `apply_offline_isolation.sh` (apply/verify/remove) in the repo and deployed to `fedora:/home/chabking/ani-ops/`.
- 2026-09-18 00:56-01:00 (Asia/Shanghai): With the three snapshots restored and the corrected isolation verified on all nodes (routing table clean / ifindex0=0, public DNS+HTTPS blocked, management reachable), the existing r6 code release + r1 artifact were transferred to `.20:/opt/ani-installer` and installed. kubeadm no longer crashed and the cluster came fully up (3/3 nodes Ready; etcd/apiserver/controller-manager/scheduler, kcn, and envoy-gateway all Running). The run still failed, but only at the final smoke task `ANI Smoke | Apply Backend` (357 total / 346 success / 10 ignored / 1 failed): `smoke/templates/backend.yaml` renders `spec.endpoints[0].ip.address` from `{{ .ani_smoke_backend_ip.stdout }}`, but `smoke/tasks/main.yaml` had no task registering `ani_smoke_backend_ip`, so it rendered `<no value>` and the Backend CR was rejected by the API server. This is an installer smoke-role defect, not a Kubernetes/containerd/kcn/Envoy defect.
- 2026-09-18 01:05 (Asia/Shanghai): Fixed the smoke role by inserting the missing task `ANI Smoke | Get backend Pod IP` (`kubectl get pod ani-smoke-backend -n ani-installer-smoke -o jsonpath='{.status.podIP}'`, `register: ani_smoke_backend_ip`) between "Wait for backend Pod" and "Render backend Service". Validated read-only against the still-live failed cluster: the command returns a valid Pod IP (`10.16.0.9`). Rebuilt the code release from the git-tracked tree (`ani-installer/kubekey`, main @ 8ad7c89) as `platform-20260918/run/ani-code-20260918-r7`: `sha256sum -c` passes, `strings kk` confirms the new task is embedded, and the binary differs from r6. Note: a second, newer but non-git-tracked tree `fedora:/home/chabking/ani-local-sync` (a different smoke architecture using inline client/network-client pods and asserts, plus divergent envoy/kcn/os-init files) was discovered and left untouched; the git-tracked tree plus the minimal validated fix was chosen as the lower-risk path to a working install.
- 2026-09-18 01:11-01:23 (Asia/Shanghai): Clean-snapshot reinstall with r7 PASSED. Restored all three target VMs with `restore_esxi_snapshots.sh execute` (3/3 reverted to snapshot id=1; the other five VMs including ani-01/02/03 and the Fedora jump host were untouched), confirmed the clean-state gate (no `/opt/ani-installer`, `/etc/kubernetes`, or `/var/lib/ani-installer`; containerd/kubelet inactive; routes clean), re-applied and re-verified offline isolation, re-transferred r7 code + r1 artifact + `site/cluster.yaml` (mode 0600) with all checksums passing, and ran `kk ani install` from `.20`. Result: `INSTALL_EXIT=0`, `total: 381, success: 371, ignored: 10, failed: 0`. Independent `verify.sh` PASSED (`VERIFY_EXIT=0`): cluster=ani-lab, registry=16 images, nodes=3, network=`ANI-NETWORK-OK`, Envoy HTTP=`ANI-INSTALLER-OK`; verify log `/var/lib/ani-installer/ani-lab/logs/verify-20260918-012311-14195-3654`. Offline isolation held throughout the install (public blocked, ifindex0=0, `ANI-OFFLINE` jump present), so this is genuine offline-install evidence. Final state: 3/3 nodes Ready (v1.35.8, containerd 2.3.4, Ubuntu 24.04.4), all control-plane + kcn + envoy-gateway pods Running, `ani-image-registry` (Hauler) active. Minimal offline first-install and real network/Envoy acceptance are PASSED.

- 2026-09-18 11:40-11:55 (Asia/Shanghai): Foundation components round 1, B0 completed (materials only; no install run). Scope is limited to cert-manager / PostgreSQL / Valkey / NATS per `docs/foundation-components-batch-execution-plan-20260918.md`. Created isolated Fedora workspace `/home/chabking/ani-installer-runs/foundation-20260918/{src,inputs,releases,logs,evidence,lab}` and transferred the current source (local tree at git HEAD 92b2bf7, clean worktree) as `inputs/src-20260918-b0.tar.gz` sha256 `d1d639ffa02b903e90c1f073b57ab9dcf652d96252fba2f2502122f048de748c`, tree digest `bb6cae2cd878e42aa347a4edbe182351bf84e28834095e33640865dda2f22927`. Note: the plan text's stated source dir `/home/chabking/workspace/ani-installer` does not exist on fedora; the local workspace snapshot was used instead. Read-only capacity check on the three clean VMs: 4 CPU / 7.8Gi RAM / 39G free root each, blank 50G sdb, ens34 UP with the management address, ens35 DOWN with no host IP for kcn, ens36 untouched, no admin.conf and no runtime dir, no concurrent install process. Re-downloaded both official charts on fedora; their SHA256 match the plan exactly (cert-manager v1.21.2 `73a56e17...`, nats 2.14.6 `72f7412d...`). Real `helm template` rendering corrected one plan assumption: cert-manager's `crds.enabled` defaults to `false`, so the installer values must set it explicitly; the controller's default args do reference `--acme-http01-solver-image=quay.io/jetstack/cert-manager-acmesolver:v1.21.2`, so acmesolver is bundled even though ACME stays off. NATS rendered with the plan's values produces a single StatefulSet with a 5Gi ani-block PVC and no promExporter/natsBox. Locked 10 component images plus the certificate tool `docker.io/alpine/openssl:3.5.4` (real command `/usr/bin/openssl 3.5.4`), recording source multi-arch index digests and linux/amd64 manifest digests separately via skopeo. Helm stays at the existing fixed v3.20.0 (tarball sha256 `dbb4c8fc8e19d159d1a63dda8db655f9ffa4aac1b9a6b188b34a40957119b286`) and will ship as artifact `bin/helm`; no upgrade. New files: `kubekey/ani/components.lock.yaml`, `config/examples/foundation-b1.yaml`..`foundation-b4.yaml`, outer `docs/foundation-components-status.md`. Base KubeKey/lower-stack versions, the APT fix, and the kubeconfig export fix are untouched. No build, no artifact rebuild, no target mutation, no install, no push; all commanded work ran through `ssh fedora`. Installation of any of the four components remains `installation_not_verified`.

- 2026-09-18 15:02-15:09 (Asia/Shanghai): Foundation components round 1, B1 (cert-manager) PASSED via clean-snapshot offline install (attempt a6). Restored only 172.16.101.20-22 (VMIDs 5/6/7, snapshotId=1, 3/3), re-applied and re-verified offline isolation (routes clean / ifindex0=0, public HTTPS + DNS blocked, mgmt SSH OK) both before and after install. Transferred the B1 code release `ani-code-20260918-b1` (kk sha256 `73078b974f6e58f0569826bb12b0623af3bc372a8cfed6b78d09f3660a4c4dc8`) and cumulative artifact `ani-artifact-ubuntu24-amd64-20260918-b1` (cert-manager Chart v1.21.2.tgz sha256 `73a56e1728edd6c99f1f31082618c3259d279a76b7ebd3d4bdc5475c2442d34a`) plus private `site/cluster.yaml` (sha256 `0a9dde898549344543e6492ab7f5f6a1e9045249359d9d69112ca5080eeebdf9`, components.certManager=true, others false). Ran `kk ani install` from .20 as the ordinary ubuntu user: INSTALL_EXIT=0, total 430 / success 420 / ignored 10 / failed 0 (end 2026-09-18T07:03:23Z). Independent `verify.sh` PASSED (VERIFY_EXIT=0): cert-manager controller/webhook/cainjector available 1/1, ClusterIssuer ani-ca + ani-root-ca (isCA) + ani-ca-test-leaf Ready, leaf SAN correct, chain verified with the offline `172.16.101.20:5000/alpine/openssl:3.5.4` tool (ANI-CERT-MANAGER-VERIFY-OK); startupapicheck hook image from offline registry; network=ANI-NETWORK-OK, Envoy HTTP=ANI-INSTALLER-OK, registry=34 images, nodes=3. Rendered verify.sh is 0700 root and values/internal-ca 0600 root (template `mode` defect fixed with explicit chmod). postgresql/valkey/nats correctly skipped. Evidence on fedora: `foundation-20260918/evidence/b1a6-{restore,install,verify,ev1,ev2}.log` plus offline re-verify log. a1-a5 were installer-defect iterations (startupapicheck hook delete-policy, jsonpath escaping, openssl 3.5 format, `{@}` vs `{.}` array iteration, unattended-upgrades dpkg lock, template mode) all fixed in source and validated by a6. No push performed. B2 (PostgreSQL) may now begin.

- 2026-09-18 17:00-19:20 (Asia/Shanghai): Foundation components round 1, B2 (PostgreSQL) — product criteria PASSED. Kept B1; added the `ani/postgresql` role (single-replica StatefulSet in `ani-platform`, official `postgres:17.11-bookworm` from the offline registry, RBD `ani-block` RWO PVC 10Gi, PGDATA subdir `/var/lib/postgresql/data/pgdata`, separate admin/app Secrets, initdb ConfigMap creating the non-superuser `ani_app` + database `ani`, and a psql-over-Service-DNS CRUD + wrong-password-rejection verification). Enabled `postgresql` in the component switch (`ImplementedComponents` = cert-manager + postgresql). Rebuilt code release `ani-code-20260918-b2` (kk sha256 `f39b4fd993845d935c5af90270a977421ad69765a471c8ef7a581a2b92623035`) and cumulative artifact `ani-artifact-ubuntu24-amd64-20260918-b2` (base runtime/ISO/hauler byte-identical to B1; adds postgres amd64 manifest digest `sha256:7bade6d532592ca8ce7ee32def7399dad2607c4ea5583839fc4352a095a11ea6`). Clean-snapshot offline install on attempt a5: restore 3/3 (VMID 5/6/7, snapshotId=1; clean-check pass), offline isolation apply+verify before and after, `INSTALL_EXIT=0`, total 446 / success 436 / ignored 10 / failed 0 (end 2026-09-18T10:53:23Z), ordinary-ubuntu entry with no manual KUBECONFIG. Independent `verify.sh` PASSED (VERIFY_EXIT=0 on the same build): cert-manager regression OK (chain verified, SAN=2), PostgreSQL app-user CRUD over Service DNS `ANI-PG-CRUD-OK`, wrong password `ANI-PG-AUTH-REJECTED`, PVC Bound, valkey/nats skipped. Lab Pod-rebuild persistence test PASSED (`B2-PERSISTENCE-OK`): record UIDs -> write unique value -> delete the single service Pod (new Pod UID) -> wait Ready + endpoint ready -> read back the same value -> StatefulSet/PVC/Secret UIDs unchanged. Installer defects fixed in B2 source and validated on the rebuilt kk: credential generator `tr -dc ... | head -c 24` SIGPIPE under `set -o pipefail` (exit 141); component verify.sh unquoted heredoc expanding `$(psql ...)` on the target host (`psql: command not found`); PVC existence check pipeline. BLOCKER (K-5, base layer, not this batch): the base cluster's kcn/OVN pod network becomes intermittently/continuously unreachable cluster-wide ~1-3 min after install (pod->pod and pod->Service ClusterIP time out), independent of the offline isolation (removing it and waiting 75s still failed) and of the product config (base runtime artifact byte-identical to B1), so a single fully-green end-to-end run is hard to reproduce (ephemeral verify Jobs landing in the bad window time out). Recommending the base/kcn owner address it before B3, since B3/B4 verification also depends on pod networking. Evidence on fedora: `foundation-20260918/evidence/b2a{1..5}-*.log`, `b2a5-install.log`, `b2a4-verify.log`, `b2a5-persist.log`, `b2a5-isolation-after.log`. No push performed.

- 2026-09-18 21:18-22:15 (Asia/Shanghai): Foundation components round 1, B2 (PostgreSQL) reinstall re-confirmation — single fully-green run (attempt a7), done at user request. Restored the three snapshots (3/3, VMID 5/6/7, snapshotId=1), re-applied offline isolation, and reinstalled from a clean snapshot as the ordinary ubuntu user with the same B2 code release (kk `f39b4fd993845d935c5af90270a977421ad69765a471c8ef7a581a2b92623035`) + cumulative artifact + site config. Added the previously-learnt lab pre-step: free the dpkg lock on all three nodes before install (`lab/preplock.sh`, 3/3 `LOCK_FREE`); the previous attempt a6 without it failed early at `Repository | Initialize Debian-based repository and install required system packages` because the boot-time `unattended-upgrades` held `/var/lib/dpkg/lock-frontend` (exit status 100) — a lab-prep issue, not a product defect. a7 result: `INSTALL_EXIT=0`, total 446 / success 436 / ignored 10 / failed 0 (end 2026-09-18T13:40:47Z), including the install's own cert-manager and PostgreSQL component self-checks. Independent `verify.sh` `VERIFY_EXIT=0` (`components=cert-manager=pass postgresql=pass valkey=skipped nats=skipped`, `ANI-NETWORK-OK`, `Envoy HTTP=ANI-INSTALLER-OK`, registry=35 images, nodes=3), with PostgreSQL `crud=ok`/`auth=rejected-wrong-password` and cert-manager chain verified. Lab Pod-rebuild persistence `B2-PERSISTENCE-OK` (write -> delete Pod -> new UID -> read back the same value -> StatefulSet/PVC/Secret UIDs unchanged). Post-install offline recheck held on all three nodes. Base caveat (K-5): the base kcn/OVN intermittently gives a recreated Pod a broken netns so the Pod IP and Service ClusterIP are unreachable from every node, while the Pod still reports `Ready` (the probe is an in-container `exec`, not network) and the endpoint still reports `ready: true`; the same Pod is sometimes reachable and sometimes not. Note this is NOT a pristine zero-intervention pass: a7 healed it by deleting/recreating the Pod (1-2 times) before the checks, after which both passed first try, so a7 must not be read as evidence that the base pod network is healthy. Evidence: `foundation-20260918/evidence/b2a7-{install,verify2,persist2,isolation-after,restore}.log`. No push performed.

- 2026-09-18 (Asia/Shanghai): Recorded an honest, standalone account of the base kcn/OVN pod-network defect as Appendix A ("附录 A") in `docs/foundation-components-status.md`, and tightened the K-5 row and the B2 status wording accordingly (the round-overview B2 row, the B2 status line, B2-5 and B2-6). Explicitly stated there: the root cause was NOT determined and the trigger conditions/failure probability were NOT quantified; the "recreate the Pod until reachable" action (`lab/b2-heal.sh`) is a lab workaround that masks the defect, not a fix; the defect is not limited to this batch's component (coredns/smoke Pods reproduced it); the earlier a1 form (host->pod fully broken on node3, taking coredns down) does not even require us to delete a Pod; and a7's green run required that intervention, so it must not be cited as proof of a healthy base pod network. Ruled out in writing: offline isolation (removed, still failed after 75s), this batch's materials (base runtime artifact byte-identical to B1), endpoint-propagation delay (waited for `ready` and retried), node resources, and DNS configuration. No component source/image/config was modified (scope boundary upheld). No push performed.

- 2026-09-18 22:19-00:30 (Asia/Shanghai): Foundation components round 1, B3 (Valkey) — code/materials complete and largely verified, batch STOPPED on a base Ceph blocker after three attempts. Source: added `builtin/core/roles/ani/valkey` (single-replica StatefulSet in `ani-platform`, official `valkey/valkey:8.1.10-alpine`, 2Gi ani-block RWO PVC, AOF everysec via a config-file Secret so the password never appears in pod args/logs — the image's entrypoint chowns /data and drops to valkey 999:1000, verified against the exact tag on the build host), enabled valkey in `ImplementedComponents`, added the images.tsv row (amd64 digest `e55eb7bf...` re-verified against the live registry), wired the role into the playbook, and reduced the not-implemented test to nats only. Built `ani-code-20260918-b3` and cumulative artifact `ani-artifact-ubuntu24-amd64-20260918-b3` (36 images; base runtime/ISO/hauler unchanged; hauler store built by loading the b2 archive and adding only the valkey image). Attempt a1 (clean snapshot, fully offline): install reached `total 450 / success 439 / failed 1` with BOTH the postgresql and valkey roles executed — valkey rollout OK, PVC Bound, and the authorized client Job PASSED (authenticated SET/GET of a unique value over Service DNS + TTL expiry). The single failure was installer-side: the negative-auth Job branched on the exit code, but valkey-cli exits 0 even for server error replies (verified against the exact image), and the exit-code form also had a false-pass hole (connection failure would read as "rejected"). Fixed to assert the RESP error code (NOAUTH/WRONGPASS) and fail on everything else; kk rebuilt as `046818239f40cbf5897e8b21761d2391592b2aeac60e0f7223fc74665df6e377`. After a1, the lab watcher's Pod-rebuild persistence test returned rc=0 (write unique key -> WAITAOF -> delete Pod -> rebuild -> read back -> UIDs unchanged), but only the rc summary was preserved (the detailed log was overwritten by a later watcher run; per-attempt evidence naming is a recorded process lesson). Attempts a2 and a3 both failed in the BASE Ceph layer before any component code ran, with two distinct mechanisms: a2 = the RBD test PVC was created 5s before `ani-block-pool` finished initializing (`pool not found`, rook operator log timestamps as proof); a3 = the rook operator wedged mid-reconcile during CSI key creation (zero log lines for >20 min, process alive with 0 restarts, 0 mgr/0 OSD pods, CephCluster stuck Progressing) until the install's own wait timed out. Per batch rules (component/base blocker, no blind reinstall) the round is STOPPED with the a3 failure scene retained; the user-directed K-5 ladder (restart kcn-controller first) was considered and does not apply (the wedge predates any datapath dependency). Evidence: `foundation-20260918/evidence/b3*` (sanitized), incl. `b3a2-ROOTCAUSE.md` and `b3a3-ROOTCAUSE.md`; known-issue K-7 (base Ceph) and K-8 (installer negative-auth fix) recorded in the status doc. No push performed.

- 2026-09-19 00:37-00:57 (Asia/Shanghai): B3 (Valkey) PASSED — single fully-green run (attempt a4), after the user approved fixing the a2 root cause. The base ceph role fix: added `ANI Ceph | Wait for the RBD block pool to be Ready` (`kubectl -n rook-ceph wait cephblockpool/ani-block-pool --for=jsonpath=.status.phase=Ready --timeout=300s`) right after `Apply RBD block pool and StorageClass` (the missing pool-CR wait was the a2 race). Rebuilt `ani-code-20260918-b3`: kk sha256 `938d34ba1f0f425ce798fb0c851ab0b703b1e2848591ba42054123f3498318d0` (artifact unchanged). a4 full clean cycle (restore 3/3 VMID 5/6/7 snapshotId=1, clean-check, preplock 3/3 LOCK_FREE, offline isolation apply+verify, checksum-verified transfer, ordinary-ubuntu install): `INSTALL_EXIT=0`, total 461 / success 451 / ignored 10 / failed 0 (end 2026-09-18T16:52:53Z), including the new pool-wait task and all three component self-checks. Watcher then ran sequentially with per-attempt evidence names: lab persistence `B3-PERSISTENCE-OK` (write unique key -> `WAITAOF 1 0` + `aof_enabled:1` real AOF completion evidence -> delete Pod, new UID a3890bae... on node2 -> endpoint ready in 2s -> read back the same value over the same Service -> StatefulSet/PVC/both-Secret UIDs unchanged; the K-5 heal ladder did NOT trigger) and independent `verify.sh` `VERIFY_EXIT=0` (`components=cert-manager=pass postgresql=pass valkey=pass nats=skipped`, registry=36 images, `ANI-NETWORK-OK`, `ANI-INSTALLER-OK`), including the fixed negative-auth assertion `ANI-VALKEY-AUTH-REJECTED` executed for real. Post-install offline recheck held on all three nodes. The a3 rook-operator wedge did not recur in a4; no workaround was applied and the upstream handover record stays in `evidence/b3a3-ROOTCAUSE.md`. Evidence: `foundation-20260918/evidence/b3a4-{restore,isolation-before,transfer,install,persist,verify,watch,isolation-after}.log` (sanitized). Known issues: K-7 a2 half resolved (fix applied+validated; a3 upstream part open), K-8 resolved with real execution. No push performed. B4 (NATS) may begin.

- 2026-09-19 01:04-02:55 (Asia/Shanghai): Foundation components round 1, B4 (NATS JetStream) PASSED — single fully-green run (attempt a2). Source: added `builtin/core/roles/ani/nats` (official Chart nats 2.14.6 via the packaged helm, values per the plan template: cluster disabled, JetStream fileStore on a 5Gi ani-block PVC via the chart's own pvc block, memoryStore off, natsBox off, token auth through the chart's `<< $TOKEN >>` mechanism + `container.env.TOKEN` secretKeyRef `ani-nats-auth/token` — the token never appears in args/logs/values; images pinned to offline refs via `container.image.fullImageName`/`reloader.image.fullImageName` because the chart's default short names would leave the cluster), enabled nats in the component switch (all four implemented), added 3 images.tsv rows (server/reloader/nats-box, amd64 digests matching components.lock), wired the role into the playbook, downloaded the official chart to inputs/materials/charts (sha256 `72f7412d...` matches the lock). CLI command shapes were frozen against a real nats server + nats-box 0.19.7 on the build host: `stream add --config <json>` (num_replicas key; no TTY confirmations in Jobs), `pub -J` produces the JetStream PubAck line `Stored in Stream: X Sequence: N`, `consumer add --config` with durable explicit-ack pull, `consumer next --raw --ack`, auth via the `NATS_TOKEN` env var (never in args), auth failures print `nats: Authorization Violation` while exiting 0 so the negative test asserts on content. Two installer-side defects found and fixed during a1: (1) the host-side PENDING marker assertion anchored `$` on a line that carries a suffix; (2) recorded as K-9. Built `ani-code-20260918-b4` (kk sha256 `256c0e1e75bf6cf18a6cdd52b0d029c2dbc37cf514693a685d292bd2b965d8ac`) and cumulative `ani-artifact-ubuntu24-amd64-20260918-b4` (39 images, both charts, 3.0G). a1 failed at the same fixed marker (`FAIL: second message is not pending`); a2 with the fixed kk: restore 3/3, preplock 3/3, offline isolation apply+verify, checksum-verified transfer, ordinary-ubuntu install `INSTALL_EXIT=0` (total 474 / success 464 / ignored 10 / failed 0, end 2026-09-18T18:15:08Z) with all four component self-checks passing, watcher then ran the lab persistence and independent verify: `VERIFY_EXIT=0` (`components=cert-manager=pass postgresql=pass valkey=pass nats=pass`, registry=39, `ANI-NETWORK-OK`, `ANI-INSTALLER-OK`) and the persistence run initially failed on a lab-only image-reference trap (the offline registry only listens on the installer node; pod image refs use `172.16.101.20:5000`, while the persist script had hardcoded `127.0.0.1:5000`, unreachable on node2/3 — recorded as K-10, product rendering unaffected). After switching the persist script to `172.16.101.20:5000/natsio/nats-box:0.19.7` and adding a write retry: `B4-PERSISTENCE-OK` on the live a2 cluster — write unique file-storage replicas=1 stream + durable explicit-ack pull consumer + 2 PubAcks (Sequence 1/2) + ack only the first (stream messages=2, consumer num_pending=1), delete the single server Pod (new UID dae989b4..., node2), endpoint ready in 2s (K-5 ladder not triggered), then stream/consumer/ack state verified intact and the second message delivered with exact payload match, STS/PVC/Secret UIDs unchanged. Post-install offline recheck held on all three nodes. Evidence: `foundation-20260918/evidence/b4*` (sanitized). Known issues: K-9 resolved, K-10 resolved (lab-only). No push performed. B5 (final delivery) may begin.

- 2026-09-19 (Asia/Shanghai): B5 live verification on the retained B4 cluster is BLOCKED; later validation deferred by the user. Fedora current-source ANI tests (plain and builtin) passed; code/artifact checksums and three-node offline isolation passed; ordinary ubuntu kubeconfig worked. Packaged verify exited 1: cert-manager/PostgreSQL/Valkey passed, NATS failed readiness. Root cause from CNI logs: at 02:48:33 +08 an old sandbox's repeated DEL (5bc5a6f874dc..., empty netns) deleted the replacement sandbox's NIC (d00e1ec6c583..., nic-ani-platform-nats-0-9w4zn, created 02:47:59). The host veth remained but its OVS Interface was gone; NATS localhost health was OK while all three hosts timed out to its Pod IP, causing kubelet startup-probe restarts. KCN deleteNicCR selects by Pod/interface without sandbox identity. User states a newer KCN fixes this and requested recording only; newer image fix remains user_reported_fixed/not_verified. No product changes, image replacement, service Pod deletion, snapshot restore, or heal. Temporary KCN regression-test addition was withdrawn. Current kk SHA is 84dc71a2c9ae569612aff208a7d220238bcebfb3111c0203f9209b414db15f00 (Fedora build/package/node match), differing from the historical B4 report; connections.md and final manual handoff remain incomplete. See [B5 verification report](../../docs/foundation-b5-verification-20260919.md). Evidence: fedora:/home/chabking/ani-installer-runs/b5-20260919/evidence/. No push/publication.

- 2026-09-19 (Asia/Shanghai): Second-batch observability execution plan documented at ../../docs/observability-components-batch-execution-plan-20260919.md, with primary-source metrics/logging material appendices. Cards C0-C5 cover fixed inputs, typed switches, metrics/alerts, Loki+Fluent Bit, OpenSearch+Fluent Bit, and handoff. Chart download/hash, amd64 manifest availability and static Helm rendering were checked on Fedora; no implementation or target installation was performed. New kcn material remains not_provided and live validation is deferred until user supplies the fixed material and schedules it. AGENTS/current-plan pointers updated; code/artifact separation and lab-only snapshot boundary retained. No commit/push/release.

- 2026-09-19 10:20-11:25 (Asia/Shanghai): Second batch, card C3 (Loki + Fluent Bit) — code/materials complete, render gate PASSED, live `not_verified`. Fedora attempt id `c3-feda-20260919`, work tree `/home/chabking/ani-installer-runs/observability-20260919/{src,evidence}`. Source: added `builtin/core/roles/ani/loki` (values.yaml / tasks/main.yaml / templates/verify.sh / templates/connection.md) and `builtin/core/roles/ani/fluent-bit` (same four files), each rendered with the packaged `artifact_root/bin/helm` against the chart tarball already materialised in C0. Loki values follow the plan template (`deploymentMode: Monolithic`, `auth_enabled: false`, split registry/repository/tag, `replication_factor: 1`, filesystem storage, schema v13 tsdb, `retention_period: {{ .ani.components.logging.retention_hours }}h`, compactor `retention_enabled: true` + `delete_request_store: filesystem` + persistent `working_directory`, `singleBinary.replicas: 1`, `sidecar: false`, `whenScaled/whenDeleted: Retain`, `enableStatefulSetAutoDeletePVC: false`, `read/write/backend.replicas: 0`, and gateway/test/lokiCanary/chunksCache/resultsCache/ruler/minio all disabled). Fluent Bit is a DaemonSet collecting container stdout only (tail `/var/log/containers/*.log`, `multiline.parser cri`, filesystem cursor + bounded buffer on a `DirectoryOrCreate` hostPath `/var/lib/ani-installer/fluent-bit`, `/var/log` read-only, control-plane toleration) with exactly one output selected by `logging.backend`; the opensearch branch reads `${OS_USER}`/`${OS_PASSWORD}` from a Secret reference and the loki branch sets `Auto_Kubernetes_Labels Off`. Also: `ImplementedComponents` now 7 (opensearch still absent until C4), `storage()` gained a `loki` case (its PVC is required; `fluent-bit`/`opensearch` are not storage-backed), `componentImageKeys()` gained `logs/loki` and `logs/fluentBit`, and the playbook runs metrics → backend → collector with the backend gated on `logging.backend` so only one of loki/opensearch is ever rendered. First-install switches: the fixed 8-row `components-selection.tsv` is unchanged and `Components.Selection()` still derives the loki/opensearch/fluent-bit rows from the single `logging.backend` string, so two backends can never both be true. Three genuine defects found by rendering, not by reading: (1) the Fluent Bit chart's image helper is `printf "%s%s%s" .repository $tag $digest` and has **no registry field**, so a bare `fluent/fluent-bit` would send every node to Docker Hub — the repository must carry `host/path`; this is the **opposite** convention from kube-prometheus-stack and could only be found per-chart; (2) `{{ .Values.* }}` never resolves because the installer's role values pass is `text/template` and Helm's second pass never runs on it, so `flush`/`logLevel`/`metricsPort` rendered the literal `<no value>` — replaced with literals and a cause-level test guard; (3) LogQL selectors written as Python f-strings collide with Go's template delimiters (`U+007B U+007D` read as a template start) — braces now built with `chr(123)/chr(125)`. Added `kubekey/lab/c3-render-gate/{render-values.sh,render-gate.sh,run-c3-gate.sh}` (generalised from the C2 single-role script): `gofmt`/`go build`/`go vet`/`go test ./pkg/ani`, then three values renders (loki, fluent-bit+loki, fluent-bit+opensearch) and three chart renders asserted against the workloads, with a final foreign-registry scan. Result: `gofmt` clean, `go test ./pkg/ani` ok, all three renders valid YAML, loki 441 lines / fluent-bit-loki 238 / fluent-bit-opensearch 262, per-role assertions all pass, **foreign images 0** in all three, and the chart SHA256 reproduce the locked C0 appendix exactly (loki `ddb31751a90269980332eb17ddbd67f2971fc1b0e847d6d1752a749c8e86232a`, fluent-bit `2404614ace4c7dc049b76fd39d13b695f7b66332c38095327ebaa8fc91f0b046`). Evidence: `observability-20260919/evidence/c3-render-gate/` plus `provenance.txt` (chart + per-file hashes). `gofmt` is intentionally narrowed to `pkg/ani builtin/core/roles` because two upstream files (`builtin/core/fs.go`, `builtin/core/upgrade_path.go`) are committed with CRLF. No component source/image/config touched (scope boundary upheld); no snapshot restore, no install, no target mutation; `kubectl`, three-node install and every real acceptance check remain deferred until the user supplies the fixed kcn material. No commit/push performed. C4 (OpenSearch + Fluent Bit) may now begin.

- 2026-09-19 11:00-11:25 (Asia/Shanghai): Second batch, card C4 (OpenSearch + Fluent Bit) — code/materials complete, render gate PASSED, live `not_verified`. Fedora attempt id `c4-feda-20260919`, work tree `/home/chabking/ani-installer-runs/observability-20260919/{src,evidence}`. Source: added `builtin/core/roles/ani/opensearch` (values.yaml / tasks/main.yaml / templates/{certificates.yaml,security-config.yaml,security-init.yaml,setup-indices.sh,verify.sh,connection.md}) rendered with the packaged `artifact_root/bin/helm` against `charts/opensearch/3.8.0.tgz` (sha256 `cad6c77d04ec2389be6f61b5422ce61e7264eb7145e42b4c78dbc00e9d7dbe4d`). Scope decisions: **no Grafana/Dashboards/Jaeger/ANI business/Milvus/HA**, and the security model is a real one (never demo mode) — `DISABLE_INSTALL_DEMO_CONFIG=true`, `allow_unsafe_democertificates: false`, `allow_default_init_securityindex: false`, https on 9200, PKCS#8 keys, node cert CN `ani-opensearch-node` (serverAuth+clientAuth), admin cert CN `ani-opensearch-admin`, seeded by `securityadmin.sh -cd /security-config -cacert/-cert/-key` with **no** `--enable-demo`, Basic auth backend `type: intern`. `sysctl`/`sysctlInit` disabled (`privileged: true` init container avoided), `serviceMonitor`/`plugins`/`rbac`/`networkPolicy`/`psp` off, `singleNode: true`, TLS signed by the existing internal `ani-ca` ClusterIssuer. Also: `ImplementedComponents` now 8 (all observability rows live) with a rewritten gate test that temporarily un-implements `opensearch` to keep the fail-closed path covered, `storage()` gained an `opensearch` case mirroring `loki` (both read the shared `logging.StorageClass`/`StorageSize`; `fluent-bit` stays unlisted because its scratch volume is a hostPath), `componentImageKeys()` gained `logs/opensearch` + `lab/python` + `lab/busybox`, and the playbook runs `metrics → loki → opensearch → fluent-bit` with the opensearch role gated on `logging.backend == "opensearch"`. Fixed two defects C3 had left in the Fluent Bit role for C4: (1) the OpenSearch PVC constant said `data-ani-opensearch-master-0` but the chart's `volumeClaimTemplates[0].metadata.name` is the template `opensearch.uname` = `ani-opensearch-master`, so the real PVC is `ani-opensearch-master-ani-opensearch-master-0` (StatefulSet PVC naming is `<claim template>-<sts>-<ordinal>` and here the template and the StatefulSet share the name; `ani-opensearch-master-0` is a Pod-shaped name and never a PVC) — **corrected later in H2**, where both this role and the Fluent Bit role now derive the name from the rendered StatefulSet instead of hardcoding it; (2) the OpenSearch polling used plain `http://` with no credentials, so it could never see the cluster it was verifying — now TLS with `ca.crt` from `ani-opensearch-node-tls` plus the `ani-opensearch-fluent-bit` collector credential, in both the marker waiter and the retention check. Three genuine findings from rendering rather than reading: (a) the OpenSearch chart's `opensearch.dockerRegistry` helper prepends `global.dockerRegistry` to **both** `image.repository` and `persistence.image`, so writing a registry into `persistence.image` double-prefixes the host (`192.0.2.11:5000/192.0.2.11:5000`) — this is the **opposite** convention from the Fluent Bit chart fixed in C3 and is asserted in the gate both ways; (b) `maxUnavailable: 1` is a non-empty chart default, so the chart **always** renders a PodDisruptionBudget — deliberately not "fixed" (1 with one replica is inert and 0 would block drains), the gate reports it and asserts only the genuinely absent objects; (c) `securityConfig.config.securityConfigSecret` makes the chart mount an external Secret whole and render **no** Secret of its own, which is why the eight security files live inlined in the role's `security-config.yaml` — two of them carry a bcrypt hash that can only be computed on the node. Added `kubekey/lab/c4-render-gate/{run-c4-gate.sh,render-gate.sh,render-values.sh}` (generalised from C3; `render-gate.sh` gained an `opensearch)` case and a `ROLE_ROOT` override). Result: `gofmt` clean (`pkg/ani`, `builtin/core/roles`), `go build` clean, `go vet` clean, `go test ./pkg/ani/...` ok, all four values renders valid YAML (loki 3906 B, opensearch 5099 B, fluent-bit-loki 5536 B, fluent-bit-opensearch 6125 B), loki 441 / opensearch 293 / fluent-bit-loki 238 / fluent-bit-opensearch 262 lines, **foreign images 0** everywhere, and `opensearch render gate passed` with every section OK (workload+PVC+ConfigMap objects, `volumeClaimTemplates` named `ani-opensearch-master`, demo refusals, TLS, DNs, mounted-from-the-role's-Secret, no privileged init container, nothing extra, single-node, the role's own security config complete, confined non-demo Job, both images resolve to `192.0.2.11:5000`). Evidence: `observability-20260919/evidence/c4-render-gate/`. `gofmt` stays narrowed for the two upstream CRLF files. No component source/image/config touched (scope boundary upheld); no snapshot restore, no install, no target mutation; `kubectl`, three-node install and every real acceptance check remain deferred until the user supplies the fixed kcn material — the OpenSearch image is not even in the local closure (`docker images | grep opensearch` empty). No commit/push performed. C5 (handoff docs) may now begin.

- 2026-09-19 13:00-13:45 (Asia/Shanghai): Second batch, card C5 (delivery, status, manual reproduction) — **documentation-only card; the C0-C5 code/material scope was closed here, then two real installs were attempted and both failed — see the handoff entry below, which supersedes the "code complete = batch done" reading of this entry.** Created `docs/observability-components-manual-runbook.md` as the batch's real delivery entry point, written entirely against this batch's real paths so it does not drag in the foundation batch's heal/watch/per-attempt scripts. It documents: the topology/material/credential locations and the purpose of each directory under `~/ani-installer-runs/observability-20260919/`; the fixed 8-row selection file and which example config maps to which logging backend; the standard 7 steps (restore -> offline isolation -> free the dpkg lock -> transfer -> ordinary-ubuntu install -> poll -> independent verify); **7 traps** — the 5 carried over from the foundation batch plus **two new ones found in this batch**: the log backend's PVC name (OpenSearch is `ani-opensearch-master-ani-opensearch-master-0`, with **no** `data-` prefix; the script derives it from the rendered StatefulSet — **corrected in H2**, the original text wrongly said `ani-opensearch-master-0`) and the fact that the three Charts' registry conventions are all different (with a comparison table); the real command shapes for transfer/install/verify (reusable by changing four variables in `b1-transfer.sh` / `install.sh`); **the from-zero procedure for each of the two logging backends**, including OpenSearch's fixed internal order (certificates Ready -> 9200 listening -> one-shot security-init Job -> auth/TLS check -> index template and ISM policy -> collector), the role split (admin only for first init, Fluent Bit writing as `ani-collector`), and `vm.max_map_count` set by the role through `/etc/sysctl.d/90-ani-opensearch.conf`; the manual acceptance checklist for metrics (real API queries, `vector(1) == 1` firing -> `vector(0) == 1` resolved on the same fingerprint, **no** `bool`, never POST to Alertmanager directly) and for logs (three-node stdout markers found through the collection path, read-back after a normal backend Pod rebuild, cursor survives a collector Pod rebuild) with an explicit requirement to mark `retention-expiry=not_verified` honestly; the three failure-attribution classes and the retry bar; and the safety boundary plus a script/evidence index. Section 9 is the connection guide: per-component Service DNS/port/namespace plus **Secret references only** (never credentials), and the command shape for reading a credential, **explicitly labelled as printing the value to the terminal** so it is only for interactive troubleshooting — **corrected in H2**, the original text wrongly presented it as "read without putting it on screen"; the genuinely non-printing forms are feeding the value straight to its consumer or injecting it via `secretKeyRef`. It also states plainly that the Loki / Prometheus / Alertmanager Services are ClusterIP-only with no public entry by design, that `connections.md` is written by the installer as root-only 0600 at `/var/lib/ani-installer/<cluster_name>/connections.md`, and that the foundation batch's four connection fragments are assembled by this batch without changing their business implementation. Every name in the document was verified against the code rather than assumed: the `connections.md` path from `runner.go:263,309`, the shared `ani-observability` namespace from `config.go:85`, the packaging paths (`/etc/kubernetes/ani/<component>/verify.sh` at 0700 root; `templates/connection.md` -> `work/connections.d/<component>.md`), and the collection-path proof in `fluent-bit/templates/verify.sh` (per-node marker Pods printing `ANI-MARKER-<RUN_ID>-n<i>-<host>-<node>` to stdout, confirmed against the Pods' own logs first so the script cannot self-certify from the expected value, then queried back through the backend API; the opensearch branch takes `ca.crt` from `ani-opensearch-node-tls` and `username`/`password` from `ani-opensearch-fluent-bit` for https + Basic). Status doc updated with the C5 section and a header conclusion that was later **superseded by the two failed live attempts**. No code changes in this card; no component source/image/config touched; no snapshot restore, no install, no target mutation. No commit/push/release performed.

- 2026-09-19 15:00-16:10 (Asia/Shanghai): Second batch, cards H1 (fix the two proven installer defects), H2 (fix the four OpenSearch orchestration defects) and H3 (rebuild the code package independently, reuse the artifact) — **code/pass, live `not_verified`; no node was touched.** The handoff doc `docs/observability-live-handoff-20260919.md` (commit fa55625) had recorded two clean-snapshot install attempts that both failed with `INSTALL_EXIT=1`, and both turned out to be *installer* defects, not the kcn defect. H1-a: nine tasks in `ani/metrics/tasks/main.yaml` read `{{ .ani.metrics.namespace }}`, but `.ani.metrics` is the `Components.Selection()` row carrying only `{enabled: true}` — the namespace lives at `.ani.components.metrics.namespace`. It rendered the literal `<no value>`, the shell read `no` as a redirection (`/bin/bash: line 1: no: No such file or directory`) and `kubectl apply` reported `error: no objects passed to apply`; all nine are now the correct path. H1-b: Loki's `GET /config` returns `text/plain` **YAML**, not JSON (no JSON negotiation is documented), so `json.load()` could only ever raise `JSONDecodeError`; the check now uses a stdlib scalar scanner plus a **duration-equivalence** comparison, because the live response reports `retention_period: 3d` while the role expects `72h` — string equality could never hold, so both sides are converted to seconds (self-tested locally: `3d` == `72h` == 259200). It additionally asserts `content_type text/plain`, `compactor.retention_enabled true`, `compactor.delete_request_store filesystem`, `common.replication_factor 1`, `schema_config.store tsdb` and `schema_config.object_store filesystem`. H2 fixed R1–R4: R1 the security-config Secret was applied *after* `helm --wait`, but the chart mounts that Secret as a non-optional volume, so a clean install would deadlock — it is now rendered and applied before Helm; R2 `security-init.yaml` was rendered on the node but never applied while its Job was waited on — it is now applied before the wait; R3 the PVC was waited on under the pod-shaped name `ani-opensearch-master-0`, which is never a PVC — the real name is `ani-opensearch-master-ani-opensearch-master-0` (StatefulSet PVC naming is `<claim template>-<sts>-<ordinal>`, and here the template and the StatefulSet share the name), and all three sites (role tasks, opensearch verify, fluent-bit verify) now **derive** it from the rendered StatefulSet via `{.spec.volumeClaimTemplates[0].metadata.name}` instead of hardcoding; R4 `vm.max_map_count` was set only on `kube_control_plane[0]`, leaving a pod rescheduled elsewhere unable to start — it now uses the same `loop:` + `delegate_to:` form the kcn role uses, covers every node in `k8s_cluster`, and fails explicitly if the value does not reach 262144. (`certificates.yaml`'s `ani-opensearch-master-0` is a **pod DNS name**, not a PVC, so it was checked and left alone.) The reason all of this passed every earlier check is the finding that matters most: the gates only ever rendered `values.yaml`, `verify.sh` and `connection.md` — **never the task file the node actually executes**. Three regression guards now close that class: (1) a new Go test `TestRoleTasksUseTheContextKeysTheInstallerProvides` walks every role under `builtin/core/roles/ani` and fails if any `.ani.<key>.` reference uses a key outside `{components, image_parts, registry, network, images}`; (2) `lab/*/render-values.sh` gained a `TEMPLATE_KIND` switch (default `values`, option `tasks`) and a new driver `lab/c2-render-gate/run-c2-gate.sh` plus extended c3/c4 drivers render all four roles' `tasks/main.yaml`; (3) the render program now uses the installer's real function table — `pkg/converter/tmpl` gained an exported `FuncMap()` and the generator calls `Funcs(tmpl.FuncMap())`, so a legitimate task using `default`/`toJson` is not misreported as an undefined function. The render context was also completed to mirror what `config.go` actually writes under `.ani` (`artifact_root`, `nodes`, `node_addresses`, `installer_node`) plus `.groups.k8s_cluster`, with `.item` supplied as a placeholder because it is bound per iteration by `loop:` (the kcn role ships the identical form and passed a real install); the `loop: "{{ ... | toJson }}"` substitution is unwrapped only for the YAML parse, since its quoted-JSON form is illegal YAML yet consumed correctly by the installer's loop parser, while a genuine YAML break elsewhere still fails. **Reverse-verified rather than assumed:** reintroducing the nine wrong paths makes `go test` fail with `components_test.go:1143: ... reads .ani.metrics. ... the component block is .ani.components.metrics.` and `run-c2-gate.sh` return rc=1 (rc=0 once reverted); moving the security-config apply after Helm fails with `components_test.go:1645: the security configuration Secret is applied after Helm, so a clean install deadlocks on a missing Secret` (passes once reverted). Results on fedora in the new work tree `~/ani-installer-runs/obs-fix-20260919` (source snapshot `src_tree_sha256=1d6ce6654f25331d176528cf8757f863237da5d2e8ce4ea1640eb9aa2d8811a9`): `gofmt -l pkg/ani builtin/core/roles` clean, `go build ./pkg/... ./cmd/...` clean, `go vet` clean, `go test ./pkg/ani/... ./pkg/converter/tmpl/...` ok, and **all three gates rc=0** (C2, C3, and C4 with 60 OK lines and 0 foreign images); the four tasks renders (metrics 5094 B / loki 3416 B / opensearch 11133 B / fluent-bit 4039 B) are valid YAML with zero `<no value>` and zero residual `{{`, and the metrics render shows `ani-observability` at all nine namespace sites. H3 built `releases/ani-code-h2fix-20260919` (kk sha256 `80bdb287a8bca3b9712e2f0c8f9a2c5d9a837ed54989c51ff40232e83508a5ec`, `SHA256SUMS` self-check 4/4) superseding `ani-code-c7661ca` (`e986c0ea...`), while the artifact was **verified unchanged and reused** (`SHA256SUMS` sha256 `351877e8a92c6c59c4cbd590d06c4881391f2bc5a060aa1925fd565adeadbebe`, matching the handoff exactly, 18/18 entries verified) — no image or Chart changed, so the big package was deliberately not rebuilt. `run-mode.sh` was parameterised with `CODE_RELEASE` (3 hardcoded references) so the next attempt installs the new package without editing the script. Evidence: `obs-fix-20260919/evidence/{c2-gate.log,c3-gate.log,c4-gate.log,c2-mutation.log,build-code.log,artifact-verify.log,provenance.txt}`. No component source/image/config touched; no snapshot restore, no install, no target mutation, no deletion of failed PVC/namespace, no restart loop. H4/H5/H6 remain blocked on the user supplying the fixed kcn material: only then are the two logging combinations installed completely from clean snapshots, with real metrics queries, Alertmanager firing/resolved, three-node stdout collected through Fluent Bit, and read-back after a normal Pod rebuild, keeping `retention-expiry=not_verified`. No commit/push/release performed.

- 2026-09-19 17:45 (Asia/Shanghai): **User authorized direct live testing** (stopped waiting for the new kcn material). attempt `h4loki-a1` (code `ani-code-h2fix-20260919`, loki-cumulative) had already proven H1-a fixed on a real install (metrics namespace/tasks all passed; failure moved into helm) but then hit **failure A2**: the Prometheus Operator CRD enforces `(^0|([0-9]*[.])?[0-9]+((K|M|G|T|E|P)i?)?B)$` on `spec.retentionSize` — a trailing B is mandatory (`4GiB` valid, `4Gi` rejected) while Kubernetes quantities are the exact opposite, so the two spellings are string-level mutually exclusive and the C2-era default `4Gi` passed every offline check yet bounced the whole metrics install at the CRD webhook. Fix (code pass): `pkg/ani/config.go` default `4GiB` + `validateMetrics` now requires the trailing B and strips it before `resource.ParseQuantity` comparison; example config, two render mocks and `TestMetricsRetentionSizeMustFitInTheVolume` (default `4GiB`, oversized `8GiB`, plus a new missing-B rejection case) all synced; C2 render gate gained a retentionSize-matches-the-CRD-pattern assertion. While deploying that assertion its own defect surfaced and was fixed in the same round: it read the CRD-stripped `.workloads` file *before* strip_crds rebuilt it, silently consuming the previous run's stale `4Gi` render; the assertion now runs after the rebuild. Fedora: gofmt/build/vet/test all green, C2/C3/C4 gates rc=0, new code package `ani-code-a2fix-20260919` (kk `63ebac35...`, self-check 4/4, copied to obs-live/releases), artifact reused unchanged. attempt `h4loki-a2`: restore 3/3, isolation, 22-entry transfer OK, install advanced past A2 (CR accepted, all workloads Running) but failed at verify [2/8] (`up{job=prometheus-node-exporter} series=0`). Read-only diagnostics against the still-running cluster proved the 21 active targets all up — node-exporter under job=`node-exporter` — giving **failure A3** (verify hardcoded the wrong job label; C2-era guess that no render gate can catch). Same sweep found **failure A4**: the values comment claimed the remote-write receiver was on but `enableRemoteWriteReceiver` was missing from the rendered prometheusSpec (live args showed no `--web.enable-remote-write-receiver`), which would 404 the [7/8] rebuild-marker write. Fixes: verify.sh query -> `up{job="node-exporter"}` with a comment recording it as a live runtime fact; values.yaml -> `enableRemoteWriteReceiver: true`. Everything else verify depends on was pre-verified live (3/1/3/120 series for the corrected [2/8] queries; [7/8][8/8] PVC names Bound, generated Secret present, rebuild labels match). New code package `ani-code-a3fix-20260919` (kk `6f963393...`, self-check 4/4) after go test ok and C2 gate rc=0; artifact still reused. attempt `h4loki-a3` launched (restore 3/3, isolation OK, 22 entries, INSTALL_LAUNCHED) and polling for INSTALL_EXIT. Docs: status.md A2/A3/A4 sections + header + overview table updated. No commit/push/release.

- 2026-09-19 18:00-18:40 (Asia/Shanghai): **A5/A6 fixes and attempt h4loki-a5 launched.** attempt `h4loki-a3` (code `ani-code-a3fix-20260919`) proved the A3 job-label fix live but exposed **A5**: Prometheus kubernetes_sd + first scrape are asynchronous (STS ready does not imply all node-exporter endpoints discovered with first sample stored; a2 saw series=0, a3 saw got 1 in the same race), so verify [2/8]'s four one-shot queries became 60x5s bounded polls whose pass condition is the original assertion itself (waiting for real data, not relaxing it). attempt `h4loki-a4` (code `ani-code-a5fix-20260919`) then passed [1] workloads, [2] all four query groups and [3] the temp receiver chain, and failed at [4/8] with a fake 5-minute timeout — **A6**: verify.sh's amcfg heredoc hardcoded receiver name `ani-metrics-recv` while `RECV_NAME` carries the RUN_ID suffix, so the wait loop's `grep -q "$RECV_NAME"` could never match; read-only diagnosis on the surviving cluster proved AM itself healthy (config-reloader "Reload triggered" ~80s after apply, /api/v2/status contains the loaded no-suffix receiver). Fix: both heredoc spots (route.receiver, receivers[0].name) now use `$RECV_NAME`. Verification: stripped-template `bash -n` OK, `go test ./pkg/ani/` ok, full C2 gate driver `run-c2-gate.sh` green (gofmt/build/vet/test + values+tasks renders + chart render assertions + 7 offline images + thanos exemption), A6 template marker confirmed inside the kk binary. New code package `ani-code-a6fix-20260919` (kk sha256 `f086ab1cdace32c5e74ed99b973cf12772f9dba4a20ae63308109375895a8482`, SHA256SUMS self-check 4/4 in both releases dirs, artifact reused unchanged). Ops note recorded honestly: `obs-live-20260919/src` is a stale 13:48 copy (missing run-c2-gate.sh and the post-A2 gate updates); all builds since a2fix actually came from `obs-fix-20260919/src` (15:51-16:53), which stays the single build source. attempt `h4loki-a5` (CODE_RELEASE=ani-code-a6fix-20260919, mode loki-cumulative): restore 3/3 (evidence `h4loki-restore-a5.log`), run-attempt chain launched, result to be appended.

- 2026-09-19 18:55-19:05 (Asia/Shanghai): **attempt h4loki-a5 failed at cert-manager verification, exposing A7 (experiment-preparation clock skew, not an installer bug and unrelated to A6).** The install ran 17 minutes and died at `ANI cert-manager | Run component verification` with the verify Job log showing `error 9 at 1 depth lookup: certificate is not yet valid`. Read-only diagnosis on the surviving cluster: node1 10:57:19 / node2 10:57:23 / node3 10:55:58 UTC (node3 ~81s behind; node2+node3 `System clock synchronized: no` — NTP service active but no reachable source under offline isolation, so clocks drift independently after a snapshot restore), leaf cert notBefore 10:48:59 UTC signed on a correct clock, verify Job (BackoffLimit=0, single shot) landed on node3 -> not-yet-valid -> whole install failed. a2/a3/a4 passed the same step only through landing-luck. Classification: installer behavior is fail-loud (exit 1, evidence retained in /var/lib/ani-installer/ani-lab/logs/); the defect is missing lab preparation — offline there is no time source to wait for, and the installer must not set clocks, so clock alignment is the same class of step as preplock. Fix (lab-side only, product code untouched): `run-attempt.sh` gained step `[2b/6] sync clocks on 3 nodes` — reads fedora's epoch, `sudo date -s @EPOCH` on each node (evidence `$P-clocksync.txt` with before/after), asserts skew <= 5s -> `CLOCKSYNC_OK`; backup `run-attempt.sh.pre-a7`, `bash -n` OK. attempt `h4loki-a6` launched with restore 3/3 (`h4loki-restore-a6.log`) and the same a6fix code package; result to be appended.

- 2026-09-19 19:35-20:00 (Asia/Shanghai): **attempt h4loki-a6 failed at metrics verify [5/8], exposing A8 (alert missing the namespace label; [5/8] had never executed before).** Big progress first: A2 (CR accepted), A3/A5 (real metric query polls) and A6 ([4/8] AlertmanagerConfig load, fake timeout gone) were all reverse-verified live in one install; the run died 25 minutes in at the brand-new section [5/8] `receiver never got a notification with status=firing`. Read-only diagnosis: receiver trio healthy with an empty /data (no request ever arrived), PrometheusRule evaluated (alert active in AM since 11:25:33 with run_id label), and the decisive AM alert dump showing `"receivers":[{"name":"null"}]` — the alert fell through to the root null receiver. The effective AM config (status API config.original) shows the Operator folds every AlertmanagerConfig into a sub-route that automatically also matches `namespace="<config namespace>"` (namespace isolation); the verify alert carried only run_id/severity, so it never matched. Fix (A8): verify.sh's firing rule labels now include `namespace: $NS`; the [6/8] resolved rule is sed-derived from it and inherits. Pre-validated on the surviving a6 cluster: JSON-patched the existing firing rule with the namespace label, and 90 seconds later the receiver recorded /data/req-001.json containing AniMetricsLifecycleTest and ani-ani-lab — routing, delivery and payload all proven. Verification: stripped-template bash -n OK, go test ok, new package `ani-code-a8fix-20260919` (kk `f7558694...`, SHA256SUMS 4/4 x2, template marker in the kk binary), artifact reused. attempt `h4loki-a7` launched (restore 3/3, `h4loki-restore-a7.log`); result to be appended.

- 2026-09-19 20:00-20:40 (Asia/Shanghai): **attempt h4loki-a7 died silently inside verify [5/8] after A8 was proven live, exposing A9 (verify passes node filesystem paths into the client pod).** The a8fix code ran: restore 3/3, CLOCKSYNC_OK (A7 fix effective), and [5/8]'s firing chain worked end to end — the alert carries the namespace label, AM routed it to the run receiver, and the webhook landed (/data/req-001.json, 1305 bytes, status=firing, namespace=ani-observability). Then the script vanished without a FAIL line. Faithful reproduction (reading the rendered sources first, per the earlier argv-mismatch lesson) showed dump_requests works and the fingerprint call shape `kubectl exec -i CLIENT_POD -- python3 - <node path> firing < fingerprint.py` dies with FileNotFoundError — the client pod has no node filesystem. py() executes the script inside the client pod (script via stdin, args via argv); [5/8] was the first caller to pass node file paths (fingerprint x2, silence_create), and the trailing 2>/dev/null swallowed the error, so `set -euo pipefail` killed the script silently. Fix (A9, pure verify.sh defect): restore the script's own declared contract ("every helper reads its input from argv" — nodes_up/scalar already work that way) — fingerprint.py and silence_create.py now take the JSON content as argv (`json.loads(sys.argv[n])`) and the three call sites pass `"$(cat file)"`; the three assignments gained explicit `|| fail` and lost `2>/dev/null` so a broken extraction can never kill the script silently again; no host-python3 dependency introduced (the "no host tooling" design stands). Verification: local grep proofs (three new call shapes, four A9 comments), stripped-template bash -n, go test ok, full C2 gate green, new package `ani-code-a9fix-20260919` (kk sha256 `75b9c0278a9768f08dd257906a5c4621237e0abc01dde161be32943780035a09`, SHA256SUMS 4/4 x2, kk binary contains the new fingerprint marker and all four A9 comments with zero `open(path)` remnants and the A8 marker intact), artifact reused. attempt `h4loki-a8` launched (restore 3/3, `h4loki-restore-a8.log`); result to be appended.

- 2026-09-19 20:40-21:05 (Asia/Shanghai): **attempt h4loki-a8 proved A9 live and failed at [6/8], exposing A10 (count() over an empty selector returns an empty vector, so the gone-check was unreachable by design).** The a9fix code ran clean through [5/8]: fingerprint extracted from argv content (no more silent death), receiver recorded the firing webhook at 12:47. [6/8] applied `vector(0) == 1` and then failed after its full 450s wait demanding count == 0. Read-only diagnosis (collect.sh h4loki-a8 + diag-a10.txt): the PrometheusRule object now stores `vector(0) == 1` and the receiver recorded the RESOLVED webhook at 12:48 — the alert really resolved and AM really delivered, while the verify poll kept waiting for a "0" sample that count() never emits (empty selector -> empty vector -> scalar.py exits -> n='?'). Fix (A10): query `count($alert_sel) or vector(0)` — exactly one sample in both states (1 firing, 0 gone), scalar.py contract unchanged, one-line change plus comment. Verified: stripped bash -n, C2 gate green, package `ani-code-a10fix-20260919` (kk sha256 `e16232ba9164bd23b1ac51760399c125239c5c46995436297defdccc5028f2a9`, SHA256SUMS 4/4, kk binary has the new query x2 and all four A9 markers with the old form gone), artifact reused. Ops pitfall re-confirmed: a bare scp/ssh inside a heredoc-fed ssh script eats the rest of the heredoc as stdin (the diag section silently never ran); always add `< /dev/null` to nested ssh/scp. attempt `h4loki-a9` launched (restore 3/3, `h4loki-restore-a9.log`); result to be appended.

- 2026-09-19 21:05-21:45 (Asia/Shanghai): **attempt h4loki-a9 proved A10 live ([6/8] passed) and failed at [7/8], exposing A11 (the remote-write receiver only speaks snappy-compressed protobuf; JSON is rejected at the protocol layer).** The a10fix code ran: restore 3/3, CLOCKSYNC_OK, and [6/8] completed for the first time ever — `count($alert_sel) or vector(0)` yields exactly one sample in both states, the rule resolved, AM delivered the second webhook (req-002.json at 12:48) and the same-fingerprint check passed. [7/8] ("Prometheus PVC/StatefulSet survive a normal pod rebuild") then failed writing its rebuild marker: the JSON POST to `/api/v1/write` returned **HTTP 400**. Read-only diagnosis on the surviving cluster (collect.sh h4loki-a9 + diag-a11.txt): replaying the old JSON body gives `HTTP 400 s2: corrupt input` — the receiver tries to snappy-decode whatever arrives (s2 is Prometheus 3.x's S2/snappy decoder), so no JSON variant can ever pass; remote write v1 is snappy(protobuf WriteRequest) by documented wire contract. Fix (A11): write.py rewritten pure-stdlib — varint helper, literal-only snappy blocks (leading varint raw length; single tag `(ln-1)<<2` for chunks <=60 bytes; extended form tag high 6 bits = `59+nbytes` with `len-1` little-endian in the following bytes) plus protobuf field helpers (wt2/wt1/wt0) assembling WriteRequest{timeseries=1} -> TimeSeries{labels=1,samples=2} -> Label/Sample, headers `application/x-protobuf` + `Content-Encoding: snappy` + `X-Prometheus-Remote-Write-Version: 0.1.0`. First draft had the extended-literal tag as `(nbytes-1)<<2`, which decodes length 1 and still reports corrupt input; corrected to `(59+nbytes)<<2`. **Prevalidated on the surviving a9 cluster before spending a restore**: old body 400 confirmed, new body **HTTP 204**, instant query 5s later count=1 value=424242, range query 7 points green. Verification: stripped bash -n, go test ok, C2 gate green, package `ani-code-a11fix-20260919` (kk sha256 `a2512ed2e6e7e18628fcb5d88b2d442734197dced88415cf69d5db2bacbff25c`, SHA256SUMS 4/4 x2, kk binary template markers verified). attempt `h4loki-a10` launched with the same package; result to be appended.

- 2026-09-19 21:50-22:20 (Asia/Shanghai): **attempt h4loki-a10 failed before verify at the cert-manager CA step; diagnosed as the K-5 known transient base-layer flake, not an installer defect.** The a11fix code ran: restore 3/3, then "ANI cert-manager | Apply internal CA manifests" died with `failed calling webhook "webhook.cert-manager.io": context deadline exceeded` (three Client.Timeout variants across retries). Read-only diagnosis (collect.sh h4loki-a10 + diag-k5.txt): every cert-manager pod 1/1 Running with 0 restarts and only normal image-pull/start events (webhook pod on node3, IP 10.16.0.57); at diagnosis time TCP connects from node1 to the webhook pod IP:10250 and to `cert-manager-webhook.cert-manager.svc:443` both FAIL (netns blackhole signature, consistent with K-5 — kcn/OVN pod-rebuild netns corruption recorded in docs/foundation-components-status.md appendix A), while minutes later the same script's `kubectl get ns default` webhook roundtrip returned OK — the blackhole self-healed. Classification: a5-a9 all passed this exact step and this round's code change touches only metrics verify.sh, so the installer is not implicated. Per the standing rule a transient flake allows exactly one same-package retry: `h4loki-a11` relaunched (restore 3/3, `h4loki-restore-a11.log`) with `ani-code-a11fix-20260919`; if the cert-manager step fails again the retry stops and the K-5 blocker is reported as-is. Result to be appended.

- 2026-09-19 22:20-23:20 (Asia/Shanghai): **attempt h4loki-a11 passed cert-manager (K-5 confirmed transient), completed the ENTIRE metrics verify [1]-[8/8] — A2 through A11 all reverse-verified on a real install — and failed at Loki verify [3], exposing A12 (the YAML scalar scanner attributes a deep ring default over the real common.replication_factor).** The a11fix code ran: restore 3/3, orchestration green, cert-manager CA apply passed this time (confirming the a10 failure was the known K-5 transient netns flake, and the same-package retry was legitimate), metrics verify ran end to end including [7/8]'s snappy+protobuf remote-write rebuild marker and [8/8]'s Alertmanager silence across a pod rebuild. Loki verify [3] then failed with `replication_factor is not 1`: its /config parse reported `common.replication_factor 3`. Read-only diagnosis (collect.sh h4loki-a11 + diag-a12.txt) proved the deployment is CORRECT: helm get values and the deployed manifest both carry `replication_factor: 1`, and the FULL /config dump (92243 bytes / 3569 lines — earlier reads were curl client-side truncations) contains `common.replication_factor 1` at two-space indent plus the ingester ring's effective `replication_factor 1` — common propagated. The bug is in verify.sh's parse_yaml_scalars: its docstring claims deeper nesting is skipped, but the code only skips lines with empty values, so the ring nested inside `common` — whose `replication_factor: 3` sits at deeper indent with Loki's ring default — overwrote the correct 1 read six lines earlier (12 replication_factor occurrences exist across the dump's rings). A second, transport-level defect surfaced the same round: the ~92KB chunked /config endpoint streams slowly and urllib's timeout=10 cut the body mid-read on a live fetch (reproduced from node1); a11 had merely been lucky. Fix (A12): the scanner now attributes a scalar to its section only at exactly two-space indent, keeps the schema_config.configs list-item flattening (four-space fields of a `- ` item), and skips everything deeper; the /config fetch got timeout=60 plus 3 bounded retries with the assertions untouched. Prevalidated on the surviving a11 cluster by running the extracted new scanner against the real dump: all seven values print correctly with `common.replication_factor 1`. Verification: stripped bash -n, go test ok, C2 gate green, package `ani-code-a12fix-20260919` (kk sha256 `db6605b900b7e8fe258b69cc9a3aecd61496aaa4e031c93240a603cdf985b0d0`, SHA256SUMS 4/4 x2, kk binary carries both new markers). Side observations recorded honestly: the alertmanager pod rebuilt by metrics verify [8/8] now exits gracefully (exitCode 0, SIGTERM) on a ~100s cycle counted as CrashLoopBackOff — suspected K-5 netns variant, to be watched on H5's fresh restore; and /loki/api/v1/push rejects hand-written JSON with a jsoniter 400, which is off the verify path entirely (the verify chain never pushes manually). attempt `h4loki-a12` launched (restore 3/3, `h4loki-restore-a12.log`) with the new package; result to be appended.

- 2026-09-19 23:20-00:10 (Asia/Shanghai): **attempt h4loki-a12 failed at metrics verify [7/8]'s pod-rebuild step — the K-5 base-layer netns corruption hit the rebuilt Prometheus; not an installer defect.** The a12fix code ran clean through the install and metrics verify up to [7/8] ("Prometheus PVC/StatefulSet survive a normal pod rebuild"): after the verify deleted the pod and the StatefulSet recreated it on node3, `kubectl wait` timed out with `FAIL: Prometheus did not come back after the rebuild`. Read-only forensics (collect.sh h4loki-a12 + diag-k5-rebuild.txt): the rebuilt prometheus-0 pod (node3, 10.16.0.78) has a dead network namespace — its own logs show `dial tcp 10.96.0.1:443: connect: no route to host` (outbound broken), a TCP connect from node1 to pod IP:9090 FAILs (inbound blackhole, same signature as the a10 cert-manager diagnosis), and kubelet kills it in a startup-probe restart loop, so the wait could never be satisfied. This is exactly the recorded K-5 defect (kcn/OVN occasional dead netns after a pod rebuild, docs/foundation-components-status.md appendix A): in a11 the [8/8]-rebuilt alertmanager pod was hit (graceful-exit crashloop) while [7/8]'s prometheus rebuild passed; in a12 the dice landed on [7/8] with zero code change there. The installer's fail-loud behavior is correct (it must not loop-restart pods). The A12 Loki fix was not reached in this attempt (the failure predates the Loki section). Per the standing rule one same-package retry is allowed: `h4loki-a13` launched (restore 3/3, `h4loki-restore-a13.log`) with `ani-code-a12fix-20260919`; if a K-5 pod-rebuild manifestation strikes again the retry stops and the K-5 blocker is reported as-is. Result to be appended.

- 2026-09-19 00:10-00:45 (Asia/Shanghai): **attempt h4loki-a13 completed metrics verify [1]-[8/8] clean (K-5 did not strike this round) and Loki verify [1]-[3] — the A12 scanner+transport fixes are now reverse-verified on a real install — then failed at Loki verify [4], exposing A13 (the exclusivity assertion misread the cumulative namespace as solo).** The a12fix code ran: restore 3/3, orchestration green, the whole metrics chain passed including both rebuild steps, and Loki verify passed workload/empty-query/config assertions before [4] died with `FAIL: unexpected deployment in ani-observability: deployment.apps/ani-metrics-kube-state-metrics deployment.apps/ani-metrics-operator`. Root cause: [4]'s "nothing extra was deployed" assertion assumed the loki release owns the namespace (solo assumption), but in loki-cumulative mode the metrics role legitimately deploys kube-state-metrics and the operator in the same `ani-observability` namespace; [4] had never executed before (a11 died at [3], a12 at metrics [7/8]), so the defect surfaced on first reach — same class as A12. Fix (A13): the exclusivity check now targets only the loki chart's own products — zero deploy/ds whose name contains `$RELEASE`, exactly one STS matching `^statefulset.apps/$RELEASE(-[a-z]+)?$` (the monolith), Loki Service must be ClusterIP; the metrics workloads no longer misreport. Static review of the never-executed fluent-bit verify.sh (to avoid burning another restore on a first-reach defect) found three real defects in its [5] loki retention branch — all three are exact recurrences of already-fixed bugs (urllib timeout=10 single-shot on the ~92KB chunked /config; json.load on an endpoint that answers YAML text; strict `retention_period 72h` grep against Loki's `3d` echo) — fixed by porting the proven a11-verified fetch/parse/duration-equivalence implementation (recorded as A14). Cross-checks against the a13 live cluster confirmed the fluent-bit verify's hardcoded `storage-ani-loki-0` PVC and `ani-loki` service names. Both fixes validated: stripped-template bash -n OK, go test ./pkg/ani ok, C2 render gate green; new package `ani-code-a14fix-20260919` built (SHA256SUMS 4/4 x2, kk markers: a13 line x1, a14 line x1, `config fetch attempt` x2, `in_list_item and indent == 4` x2 — both files embedded), copied to obs-live releases, dual-dir checksums identical. `h4loki-a14` launched with the new package (restore 3/3, `h4loki-restore-a14.log`, orchestration green: ISOLATION_OK/PREPLOCK_OK/CLOCKSYNC_OK/INSTALL_LAUNCHED); retry rule unchanged — another K-5 pod-rebuild strike stops the loop and reports the blocker as-is. Result to be appended.

- 2026-09-20 00:45-01:25 (Asia/Shanghai): **attempt h4loki-a14 passed metrics verify [1]-[7/8] then failed at [8/8]'s Alertmanager pod rebuild — the K-5 base-layer netns corruption struck a THIRD rebuild-class step; per the standing rule the retry loop is STOPPED and the K-5 blocker is reported as-is.** The a14fix code ran: restore 3/3 (dry-run + execute both logged), six-step orchestration green, 27-minute install, metrics verify through [7/8] (the Prometheus rebuild that died in a12 passed this round), then [8/8] died with `FAIL: Alertmanager did not come back after the rebuild`. Read-only forensics on node1 (/tmp/diag-k5-a14.sh): the rebuilt alertmanager-0 (node2, pod IP 10.16.0.78 — the SAME IP the a12-round prometheus-0 had when its netns died) is in CrashLoopBackOff with 6 restarts; node1 TCP connects to pod:9093/9094 both FAIL (inbound blackhole); kubelet's own readiness/liveness probes time out (`context deadline exceeded`) and kill the container in a loop; the AM process itself starts cleanly (Listening on 9093) and exits gracefully on SIGTERM ~100s in — zero process errors, a pure network-layer death. Contrast pod: the [7/8]-rebuilt prometheus-0 (10.16.0.77) has 0 restarts and is healthy in the same cluster in the same round. Same-cluster-same-round one-dead-one-healthy randomness plus the same IP repeatedly dying across rounds pinpoints OVN-side netns state residue: K-5 confirmed, not an installer defect. K-5 has now hit rebuild-class steps three times (a10 cert-manager webhook, a12 prometheus-0 [7/8], a14 alertmanager-0 [8/8], plus the a11 post-verify AM graceful-exit loop sign). Per the standing rule the retry loop stops here: Loki [4]/[5] and the entire fluent-bit verify chain remain not_verified pending a K-5 fix; H5 (OpenSearch) was not started. H4 final live state: metrics verify [1]-[8/8] PASSED twice (a11, a13), Loki verify [1]-[3] PASSED (a13, reverse-verifying A12), Loki [4] fixed as A13 (code), fluent-bit verify statically fixed as A14 (code); A2-A14 all packaged in `ani-code-a14fix-20260919`. Evidence: `obs-live-20260919/evidence/h4loki-a14/`. No commit/push (no authorization this batch).

- 2026-09-20 01:50-03:10 (Asia/Shanghai): **the "stop retrying" disposition was OVERRIDDEN by the user (finish the batch, do not stop); same-package retries h4loki-a15 and h4loki-a16 ran and both hit K-5 again in NEW forms.** a15 (`ani-code-a14fix-20260919`, restore 3/3, orchestration green): metrics verify [1]-[6/8] passed, then [7/8] died with `FAIL: Prometheus did not come back after the rebuild` (pre-K5_RETRY code, first rebuild dead). a16 (same package): died EARLIER — the install-time smoke step `Run active network and Envoy probe` — the active network probe pod was blackholed, i.e. the first observed K-5 hit on a FRESH non-rebuild pod outside verify (after a10's cert-manager webhook). Deep diagnostics on the surviving a16 cluster (diag-k5-deep/deep2 + a k5exp namespace experiment): 20 minutes after the failure, 4/4 freshly created pods came up healthy — at the time read as "the blackhole is a transient window that self-heals" (later corrected by a17, see below). Evidence: `h4loki-a15/{install.log,cluster-state.txt}`; a16 kept only prep logs (isolation/clocksync/transfer).

- 2026-09-20 03:10-04:05 (Asia/Shanghai): **attempt h4loki-a17 shipped the K-5-aware bounded rebuild retry (new code package `ani-code-a17fix-20260919`, kk sha256 `8b415702503db1bf…`) and proved the retry fires in production — but a plain second rebuild does NOT clear the blackhole (new IP still dead), forcing the recovery design upgrade.** The retry engineering: metrics verify gained `k5_rebuild_wait` (rollout timeout -> record `K5_RETRY` stdout + `k5-retries.txt` evidence -> delete the dead pod -> one more bounded rebuild -> still failing fails the run), applied at [7/8] and [8/8]; fluent-bit verify [3]/[4] got the same shape. The a17fix run: restore 3/3, orchestration green, metrics [1]-[6/8] passed, [7/8] triggered `K5_RETRY prometheus: … rebuilding it once more` (first confirmed firing), the second rebuild ALSO timed out at 900s -> FAIL after 42 minutes. Read-only forensics (diag-k5-a17.sh + the surviving cluster): the dead prometheus-0 second instance has pod IP 10.16.0.81 (first instance .80 — the "same IP reuse" hypothesis is dead), its own log streams `dial tcp 10.96.0.1:443: connect: no route to host` (outbound broken), node1 cannot learn its ARP entry and pings fail (inbound broken), while same-node neighbors (alertmanager-0 10.16.0.76, ani-metrics-client 10.16.0.78) are perfectly healthy — same node, one dead one alive, per-pod granularity. Evidence `evidence/h4loki-a17/{install.log,cluster-state.txt}`.

- 2026-09-20 04:05-04:40 (Asia/Shanghai): **a live recovery experiment on the surviving a17 cluster pinned the K-5 mechanism and produced a working recovery recipe; it is now engineered into both verify scripts as a third retry stage (new code package `ani-code-a18fix-20260919`, kk sha256 `a6dca041b0f606b8…`).** Experiment (evidence `evidence/h4loki-a17-k5recovery-exp.log`): the bad pod's third instance (10.16.0.82, born when the v2 probe script's delete completed just before the script was killed and the STS rebuilt) was ALSO blackholed at baseline — the blackhole had survived THREE consecutive rebuilt instances (.80/.81/.82) across 35+ minutes, pinned to the pod slot, while same-node controls pinged OK. OVS forensics found the smoking gun: br-int table=79 carries EXPLICIT anti-spoof drop flows keyed on the pod's own MAC/IP (`dl_src=fa:00:5b:14:0f:50, nw_src=10.16.0.82`, 213 packets dropped) — the datapath judges the pod's traffic as arriving on the wrong port and discards it, and the stale binding survives pod recreation. Q1 (restart node3 kcn-cni-ds alone): NOT recovered. Q2 (+ restart kcn-ovs-ds): NOT recovered. Q3 (restart both agents, then delete the pod once more so the STS replacement is set up by a fresh CNI add): RECOVERED — the fourth instance (10.16.0.83, same node3) reached Ready in ~20s with TCP/ping fully OK. Engineering: metrics verify `k5_rebuild_wait` now has a third stage (restart the kcn data-plane agents on the dead pod's node via new `k5_dp_agent_restart`/`k5_dp_agent_wait`, then delete + final rollout; call sites now 600s/420s/600s), and fluent-bit verify [3]/[4] got the same shape (`k5_dp_agent_restart`/`k5_dp_agent_wait`/`k5_dp_pod_node`); every intervention is recorded in `k5-retries.txt` (`k5_retry` / `k5_retry_dataplane` lines). Verification: stripped-template bash -n both files, `go test ./pkg/ani/` ok, C2 render gate passed, package verified in BOTH releases dirs (SHA256SUMS all green x2, kk marks: k5_dp_agent_restart x5, k5_retry_dataplane x3, 600s 420s 600s x2). Attempt `h4loki-a18` launched (restore 3/3 `h4loki-restore-a18.log` -> `CODE_RELEASE=ani-code-a18fix-20260919 bash lab/run-attempt.sh h4loki-a18 loki-cumulative`); result to be appended.

- 2026-09-20 05:45-05:55 UTC a18 结果与 a19fix：attempt h4loki-a18（包 ani-code-a18fix-20260919）INSTALL_EXIT=1 —— Loki verify [1][2][3] 全过后，[4] 在 workload 列表打印后、任何 FAIL/note 之前静默退出（缺陷 A15：`set -euo pipefail` 下健康状态即 grep 无匹配 → exit 1 → 脚本被杀，无 FAIL 行；l20 [4] 排他断言两条 grep 管道首次真实执行即踩雷）。同轮暴露缺陷 A16：kubectl v1.35 的 `rollout status` 退出码不可信（node1 实测超时打印 error 却 RC=0；删 pod 后 STS status 滞留 readyReplicas=1 会瞬间"complete"）——metrics [7/8] prometheus pod 27 分钟从未 Ready（node2 首例 K-5）却被假通过；[8/8] alertmanager 走完 k5_retry(21:02:16Z)→k5_retry_dataplane(21:09:17Z)→重启 node3 kcn agent→删 pod→新实例 .80 约 40s Ready 的三阶段恢复链并真实通过（a17 恢复配方端到端实战验证成功，K-5 在 node2 同样成立）。a19fix（包 ani-code-a19fix-20260919，kk 4f5a1b4fbaabe226）：metrics verify 新增 k5_pod_ready（kubelet 写的 pod Ready 条件轮询）与 k5_workload_ready（readyReplicas/numberReady 轮询），k5_rebuild_wait 三段+[1/8] 四 workload+webhook receiver 全部弃用 rollout status 退出码；fluent-bit [3] backend 三段同型替换（[4] 的 kubectl wait 本就是 pod 级信号不动）；loki [4] 两条 grep 补 || true、[5] delete 补 || fail。门禁：C2 渲染门禁 PASS、go test ./pkg/ani/... PASS、bash -n ×3 PASS、双 releases SHA256SUMS 全绿。restore 3/3 后 h4loki-a19（loki-cumulative）于 05:5x UTC 发射，结果待补记。

- 2026-09-20 06:1x-07:0x UTC a19 结果 + a20 结果 + A17 修复 + a21 发射：h4loki-a19（包 ani-code-a19fix-20260919）死于 cert-manager `Apply internal CA manifests`——API server 调 webhook 连续 4 次超时；判别矩阵 + OVS 取证定性 **K-5 Form B**（源节点 node1 的 host→overlay 出向整体死亡，与目标 pod 无关；node2 反 spoof drop 恰在失败时刻吞 97 包），重启 node1 自身 kcn agent 后 FAIL→OK，K-5 恢复总配方确立（重启"路径断裂那一侧"节点的 kcn 数据面 agent；Form A 在目标节点+删 pod，Form B 在源节点不动 pod）。h4loki-a20（同包，restore 3/3，22:41 UTC 失败）：**首次真实到达并全过 metrics verify [1]–[8/8]（A16 修复后的 pod-Ready 轮询版）与 Loki verify [1]–[3]（HTTP API/retention 逐项核对）**，死于 Loki [4] 排他断言——**缺陷 A17（A15 修复引入的回归）**：`loki_sts` 捕获加 `| tr '\n' ' '` 合并行，单匹配时留尾随空格，精确比较必然失败（错误消息自相矛盾 "expected ani-loki, found: statefulset.apps/ani-loki "）。修复：loki_sts 去 tr（|| true 独立防 pipefail）；全仓排查仅剩 loki_extra 一处（-z 检查无害）。附带观察：Loki [4] 列表里 prometheus STS 0/1（metrics 通过后 K-5 残留再次劣化，与 a18 后 AM 劣化同型，restore 重置，非 verify 缺陷）。门禁：bash -n + c4 渲染门禁 values/tasks 双绿；新包 ani-code-a20fix-20260919（kk 29f7a32be382c454，内嵌 A17×5 + k5_pod_ready×8，SHA256SUMS 全过）。restore + a20fix 包重跑 **h4loki-a21**（loki-cumulative 全链，fluent-bit 段待首次真实执行），发射中。

- 2026-09-20 07:0x-07:5x UTC a21 结果：**K-5 Form C 定性（host→本节点 pod 双节点持久死亡）→ 恢复配方对该形态无效 → a22 重跑**。首发插曲：run-attempt 报 code release missing（新包漏同步 obs-live releases，"双 releases"约定），拷包校验后跳过重复 restore 重发。编排全绿后 **23:00 UTC 死于安装时 smoke**（a16 同款位置，verify 之外）：`network client reached Failed phase`。存活现场取证：client（node3）日志 NETWORK-POD-IP-OK + NETWORK-SERVICE-IP-OK 已过、死在 DNS `bad address`；**node3 client → backend pod/Service 全 OK；node1 → 本节点 backend(10.16.0.9) FAIL；node1 → node2 coredns pod IP OK；coredns 双 pod（node2）0/1 crash loop（kubelet probe 死）**。定性：**Form C = host→本节点 pod 路径持久死亡，node1+node2 同时中招**（与 Form A 单 pod 槽位不同：断点在宿主侧且多节点；与 Form B 不同：跨节点 host→pod 正常）。两阶段恢复实验（k5formc-exp/exp2，第一阶段 ovs-ds 因 -o wide 列错位漏删 cni-ds、第二阶段 jsonpath 补齐）：重启 node1+node2 cni-ds+ovs-ds 后 coredns pod IP 直连恢复，但 **node1→本节点 backend 仍 FAIL、kube-dns ClusterIP 仍死、node2 probe 仍崩——Form C 对 agent 重启配方免疫**。推论：smoke 加 K-5 自动恢复无工程价值，遇 Form C 只能整轮 restore。处置：restore + 同包 a22（A17 修复仍未获执行机会）；smoke K-5 命中统计 a16/a21 两死、a17/a18/a20 过（概率性环境病）。

- 2026-09-20 08:0x-08:4x UTC a22 结果 + A18 修复 + a23 发射：h4loki-a22（包 ani-code-a20fix-20260919，restore 3/3，23:32 UTC 失败）**全程推进至 fluent-bit verify [2]**：smoke 过（K-5 未命中）→ **metrics [1]–[8/8] 二连过 → Loki [1]–[5] 全过（[4] 排他断言 A17 修复首次真实执行通过）→ fluent-bit [1] 过** → 死于 [2] `exec collector -- cat /fluent-bit/etc/fluent-bit.conf` → `cat: executable file not found in $PATH`。**缺陷 A18：fluent-bit 5.1.2 镜像 distroless（无 shell/coreutils），exec 进 collector 必败**——三处死点（[2] cat + [4] 两处 sh -c ls cursor 指纹）。修复三处全改"不进容器"等价断言：[2] 从 collector pod spec 的 configMap 引用解析（`{.data}` 键名匹配）经 API 读 `{.data.fluent-bit\.conf}`；[4] 新增 cursor_ls()——busybox inspector pod（离线 registry 内 $BUSYBOX_IMAGE）钉 victim 节点、挂同一 hostPath /var/lib/ani-installer/fluent-bit readOnly、ls 后取 logs、phase 轮询防不可信信号、用后即删。排查面：metrics/loki verify 无 exec 组件容器（a22 已真实全过）、fluent-bit 其余 exec 进自己的 python client（有 sh）、opensearch verify 仅 exec 自己 client（H5 无此雷）。门禁：bash -n + c4 双门禁绿 + `{{` 占位符审计（6 处全原有）。新包 ani-code-a22fix-20260919（kk 0da3b88b0892ca27，内嵌 A18×9/cursor_ls×3/CONF_CM×6/k5_pod_ready×8，SUMS 全过）；吸取坑 #21 先同步 obs-live releases（LIVE_SYNC_OK）。restore + a22fix 包发射 **h4loki-a23**——若 fluent-bit [2]–[5]（ConfigMap 链 + inspector）全过即 H4 收官。

- 2026-09-20 09:0x UTC a23 结果 + A19 修复：h4loki-a23（包 ani-code-a22fix-20260919，restore 3/3，00:39 UTC 失败，运行约 36 分钟）——**A18 修复（[2] ConfigMap 链）首次真实执行通过**，推进到 [2] marker pod heredoc apply 时炸：`yaml: line 14: did not find expected '-' indicator`。**缺陷 A19（从未执行段第一颗雷）**：marker heredoc 的 args 块序列行尾多 `]`（`...; done"]`），post-marker heredoc 同病——两段从未真实执行过（a22 死在 [2] 更早的 exec），首次执行即炸。修复两处 `; done"]`→`; done"`；**新增 heredoc YAML 模拟校验**（抽取全部 apply-heredoc + 样例值替换 + yaml.safe_load_all）：fluent-bit 3 个 heredoc 全 OK；顺手查 opensearch verify：0 个 apply-heredoc（H5 无此雷）。新包 ani-code-a23fix-20260919（kk af690e7e61bacc2f，`done"` ×6 实锤）+ LIVE_SYNC_OK。

- 2026-09-20 09:4x-10:3x UTC a24 结果 + A20 修复 + verify 渲染门禁落地 + a25 发射：h4loki-a24（包 ani-code-a23fix-20260919，restore 3/3，01:16 UTC 失败，运行约 32 分钟）——A19 修复生效（heredoc apply 过了），推进到 [2] marker pod 等待：`kubectl wait --for=condition=Ready pod/ani-log-marker-1 --timeout=180s` 超时。**存活现场取证推翻 K-5 猜测：三个 marker pod 全部 InvalidImageName，事件显示 image tag 渲染成了 Go 模板 nil 值**——**缺陷 A20：verify.sh 的 `index .ani.images "docker.io/library/busybox:1.37"` 键名与 images.tsv 的 `busybox:1.37.0` 差一个 `.0`**，index 返回 nil 渲染成 `<no value>`；verify.sh 不在渲染门禁内，键 typo 无任何离线检查能抓。逐键校验 8 个 role verify 引用的 7 个镜像键：**唯一缺失就是 busybox:1.37**（python/openssl/nats-box/postgres/valkey 全在表）。修复三件套：① verify.sh 键名对齐 1.37.0；② **渲染门禁落地 TEMPLATE_KIND=verify**（render-values.sh 三副本同步：verify 分支 + 产物级 `<no value>`/`<nil>` 扫描 + bash -n + .sh 输出防呆放宽）——H1（tasks 进门禁）之后的第二个"未渲染文件"教训补齐；③ 门禁全量 8 role verify 渲染 8/8 PASS + opensearch backend 分支 2/2 PASS。插曲：A20 注释文本里写了 `<no value>` 字面量被门禁产物级扫描拦截（乌龙）——注释也不能含该字面量，已在注释里说明。新包 ani-code-a24fix-20260919（kk cce36749aa74d171，SUMS 全过）+ LIVE_SYNC_OK。restore + a24fix 包发射 **h4loki-a25**：fluent-bit [2]–[5] 全过即 **H4 收官**。

- 2026-09-20 10:0x-10:3x UTC a25 结果 + A21 修复（一函数三雷）+ 真实数据离线复验 + a26 发射：h4loki-a25（包 ani-code-a24fix-20260919，restore 3/3，01:59 UTC 失败，运行约 22 分钟）——**采集链路本体首次真实通过**：A20 修复生效，三个 marker pod（node1/2/3）全部 Ready，`await_markers.py` 在 client pod 内查 Loki **三枚 marker 全部查到**（namespace/pod/container/node 标签齐全，tee 写盘 markers-found.txt 3090B 成功）——容器 stdout → 运行时日志 → collector tail → Loki 可查这条 C3 核心链路真实走通。死于 [2] 元数据校验：`FileNotFoundError: /var/lib/ani-installer/ani-lab/logs/fluent-bit-verify-20260920T015654Z/markers-found.txt`（`command terminated with exit code 1` 实锤 kubectl exec 执行位置）。**缺陷 A21：check_metadata.py 三雷同发，全部属"从未执行分支"**——① 宿主路径传入 pod（py() 经 kubectl exec 在 client pod 内执行脚本，第一参数却是 node1 宿主机路径）；② 参数切片错位（`nodes = sys.argv[2:]` 吞入尾部 NS，`expected_ns = sys.argv[2+len(nodes)]` 越界）；③ 序号解析错误（`rsplit("-n",1)` 切在 `-node3` 的 `-n` 上 → `int("ode3")` ValueError）。修复：① 查询结果先 `kubectl exec cat >` 上传进 pod、checker 读 pod 内路径（node 侧 tee 文件保留为证据）；② 切片改 `sys.argv[2:-1]`/`sys.argv[-1]`；③ 序号 `re.search(r"-n(\d+)-", marker)` 锚定。顺手补仓库同步缺口：渲染门禁 verify 扩展此前只改了仓库 c4 副本，c2/c3 仓库副本补齐三副本一致。**真实数据离线复验**：从 node1 sudo 拉回 a25 真实 markers-found.txt 跑修复后 checker → RC=0，三枚 marker 四项元数据断言全过（a25 若带修复 [2] 即通过）。门禁 8/8 verify PASS。新包 ani-code-a25fix-20260919（kk c5e184e5fab9627c，SUMS 全过）+ LIVE_SYNC_OK。restore + a25fix 包发射 **h4loki-a26**：[2] 自 check_metadata 起为新执行面，[3][4][5] 仍未真实执行，全过即 **H4 收官**。

- 2026-09-20 10:3x-11:0x UTC a26 结果 + K-5 post-Ready 变体定性 + A22 修复（断言级恢复重入）+ a27 发射：h4loki-a26（包 ani-code-a25fix-20260919，restore 3/3，02:37 UTC 失败，运行约 21 分钟）——**A21 修复真实执行通过：fluent-bit [2] 全过（上传进 pod + 三雷全排）、[3] backend pod 重建恢复也过**（PVC/UID 不变 + 旧 marker 复读通过）→ 推进到 **metrics [7/8]** 死于 `FAIL: the pre-rebuild sample is not readable after the rebuild`（urllib TCP connect 超时）。**K-5 新变体实锤（post-Ready netns 死亡）**：重建的 prometheus-0（node3 10.16.0.77）短暂 Ready → k5_rebuild_wait 通过（无 K5_RETRY）→ ~30 秒后 netns 死亡：容器自身日志 `dial tcp 10.96.0.1:443: connect: no route to host`（出向）+ node1→pod IP 入向黑洞 + kubelet 探测全超时 + **ani-metrics-prometheus Service Endpoints 清空** → range 查询打到空后端 ClusterIP 必超时。Ready 翻 False@02:37:05、FAIL@02:37:23；容器 restart=1（liveness 杀、优雅 exit=0）、无 OOM（未设 limits）。**k5_rebuild_wait 盲窗 = pod 级 Ready 通过后、断言执行前的窗口内 netns 死亡**（与 a12/a17"从未 Ready"形态互补）。**缺陷 A22 修复**：metrics verify 新增 `k5_read_again`（读断言失败 → 记 k5_retry_sample_read → 删 pod 重走 k5_rebuild_wait 三阶段 → 重读；样本在 PVC 二次重建无损），[7/8] range 查询与 [8/8] silence 读回（抽出 am_silence_poll）同型包装。**测试夹具滞后修正**：TestFluentBitVerifyProvesCollectionPath 仍断言 A20 修复前 `busybox:1.37` 旧键 → 改 `busybox:1.37.0`（教训：修 verify 键必须同步 grep 测试夹具键字面量；build-code.sh 不跑测试）。门禁：8/8 verify PASS + bash -n + go test ./pkg/ani/ ok。新包 ani-code-a26fix-20260919（kk dff7cfe8f834643f，SUMS 全过）+ LIVE_SYNC_OK。restore + a26fix 包发射 **h4loki-a27**（残留：fluent-bit [3]/[4] 有同型 post-Ready 盲窗，本轮不动，被击中再包装）；全过即 **H4 收官**。

- 2026-09-25 23:00 → 2026-09-26 (Asia/Shanghai): **R 整改轮 F01–F12（代码阶段，无实机）**。基线 HEAD f58430e，任务包 docs/execution/remediation/20260925/（含隔离复现证据 zip，SHA256SUMS 5/5）。六组连续推进 G3→G1→G2→G4→G5→G6，每组跑统一门禁并记树指纹：422aa916 / c6c76138 / e03c610b / 5e810a83→5b16c585 / 6015fe32 / c3310821 / bb326651（全部 rc=0）。要点：smoke 与 acceptance 真实分层（ANI_VERIFY_LEVEL 被脚本读取，重型链路只在 acceptance，超时二次重建补救全删）；至多一次意图账本 + 共享产品锁 + Ready 条件 + PG/NATS 真实数据读回 + 取消杀进程组（30s→5.2s）；组件计划/执行共享写前检查（成功底座记录、集群 UID+节点集、CNI 健康、闭包重算、所有权漂移、材料锁、子 kk 摘要、执行主机），Helm 改 create-only + ani.io/managed-by 归属；身份合同拆成六字段并构建期注入树指纹；真实预检（制品摘要先于锁与目录、chart 按条目绑定、ISO 记录严格化、systemd 单元语义纠正）；材料链迁入 Go（place-tools/place-charts/verify-registry/inject-repository-iso + 首装/新增共用 registry 内容门，index 递归平台 manifest 与 blob）；render 与 playbook 选择奇偶校验、heredoc 三分类、逐行 <no value>、同文件重复资源、真实 helm 展开入口与内嵌角色树默认源；门禁补 -tags builtin 构建/vet/test、connector/cmd 测试、python 模块预检与 CI 触发路径，并以三处 Go 故障注入证明新覆盖真的在跑；冷启动改为按内容校验的 reboot 证据（单元/主机/前后 bootID/端口/真实冷拉字节）。测试：pkg/ani 265+ 例全绿，新增 f_remediation_g1/g2/g3/g4/g5/g6 与 r15/r13/r14/registry_verify/template 适配（含把“期望 1 次 delete”改强为“0 次”等），test-build-offline-materials 重写为端到端制包回归（含 beta-in-alpha 复现、同名不同内容 ISO、store 内容门），test-ceph-storage-safety 扩至 36 例。同源代码 release 由 scripts/build-code.sh 在与门禁同一棵树上构建校验；未提交、未推送，故不写 ci_pass；选定组合实机复验与真 helm/真 hauler 路径待授权（详见 ../ani-installer-work/evidence/F-remediation-20260925/WORKLOG.md）。

# F-live closeout — 2026-09-26 (MODE=live, nothing committed)

- Two live defects were found by real installs and fixed with regressions written red first: L-04 (the Envoy
  smoke client raced per-node kube-proxy programming; the probe now retries a bounded number of attempts
  inside a bounded wall-clock window, each attempt a fresh Pod that must produce both markers on its own
  stdout) and L-05 (manifest-owned components stamped `ani.io/managed-by` only on their Service, so the
  installer could not recognise its own StatefulSet and the same-version read-only no-op was unreachable).
- L-06 is closed in code and verified against the running base: `kk ani components execute` now lands a
  `components-execution` record bound to its base install record and to the live cluster; `kk ani verify`
  consumes that record type for smoke and refuses a plan, a failed pass, a torn or tampered file, a foreign
  cluster, a changed base, an incomplete identity, or an `--only` outside what the record attests. An
  observation (`noop`) record grants verification scope only, never a declared recreation, and the change
  budget stays keyed to the base install run. The first install's own record and verify path are unchanged.
- Re-auditing that closeout against its own claims, rather than reading the ledger back, found six rules
  that were announced but not enforced, and each is now enforced where an operator would meet it:
  a record's kind is decided by the `componentsExecution` block, not by its `recordKind` string, so a
  re-labelled execution record is refused both by `ani verify` and as the `--base-run` of a later addition
  (it would otherwise skip every base/evidence/identity check and re-key the one-shot delete budget to its
  own sub-run); a pass that already changed the cluster but cannot aggregate its connection facts lands a
  failed record instead of nothing; the scope a record grants must equal its own targets, and every name
  that becomes a path must be a single safe element; an empty attested scope is refused rather than
  reported as a pass over zero checks; the writer will not land a record its own consumer must reject; and
  the read-only pass honours the plan's binary binding like the add pass does.
- `metrics` was bound to StatefulSet `ani-metrics-prometheus` while its role rolls out
  `prometheus-ani-metrics-prometheus` (read back from the live cluster). A uid read of a workload that
  never exists yields no identity, the target degrades to `unknown`, and the pass would exit non-zero after
  installing, or land a record its consumer refuses. Every component spec is now checked against the
  workloads its own role names, which is red on the old metrics value and green on the corrected one.
- Remaining limit on the record vocabulary, stated plainly: `cancelled` and `unknown` are refused by the
  consumer and representable in the record, but the executor never produces them — an interrupted
  components run is recorded as `failed`, and a partially executed playbook cannot say which component ran,
  because the playbook is one unit. The `operation=add` branch has not been executed on live (it would need
  the existing valkey removed or an unselected component enabled); it is covered by the production writer
  feeding the real consumer in test, and is recorded as not run on live, not as a pass.
- Offline cold start: one further registry VM reboot was taken with a lab-only boot isolation unit installed.
  It did not survive the boot (`network-pre.target` is never reached on these nodes, so the unit never ran);
  cross-reboot automatic egress isolation stays unverified and node1 had a public-egress window after the
  boot until isolation was re-applied by hand. After that restore, empty-cache pulls of one byte-exact and
  one platform-selected row succeeded on the isolated registry host and on an isolated client, every blob
  hashed to its declared digest and equal to the packaged evidence, with no shared cache deleted.
- Release identity is deliberately not stated here: this file lives inside `kubekey/`, so any number quoted
  about the build that ships it would invalidate itself. The full-length source tree fingerprint, binary
  digest and per-stage exit codes are recorded in `../docs/execution/progress.yaml` (task `F-LIVE`) and in
  the out-of-tree ledgers under `/home/chabking/ani-installer-runs/f-live-20260926/`.
