# B08.V｜Notebooks 控制器与CPU工作区

本次仅执行：**只做本批已授权实机验收；M/I/T和所有前置必须有证据**。

先读本目录`B08.md`中的对应阶段、本包`05-model-instructions.md`和已填写的task-result。前置为B00；

本阶段还必须具备B08.T的有效结果。实机V还需父卡列出的全部已选底座/组件前置，并通过R16。

禁止跨到下一阶段。所有拟新增命令/文件先检查实际存在，不能通过“没有文件所以跳过”获得成功。T阶段未通过不能启动快照还原。V阶段失败保存只读证据后停止，不重启底层网络、不删PVC、不解除暂缓项。

使用`templates/task-result.yaml`回报。源代码/物料/现场问题分别归因，缺支持证据写blocked。只有新的具体修正并通过前置测试后，lab才可发起下一次不同attempt。不要自动提交Git或对外发布。
