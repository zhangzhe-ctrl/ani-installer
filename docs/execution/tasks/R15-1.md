# R15.1｜新增组件纯计划与拒绝路径

前置：R05,R08,R09,R13,R14。父任务：[R15](R15.md)。状态：未开始。

本次范围：只接入CLI解析、--only闭包、scope/owner预检和计划输出。fake connector证明不会调用create_cluster/CNI/Ceph。禁止集群变更。

先读父任务的允许文件、对应步骤和全部停止条件；只实现本子卡覆盖部分。未覆盖用例标not_run，不能将父任务标done。按05规则报告实际命令、退出码与证据。

只有本卡通过并获分配后再执行R15.2，不自动跨卡。失败无底层重启/清盘/删PVC/重放未知远程命令。此卡不自动Git提交或对外发布。
