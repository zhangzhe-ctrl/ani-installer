Goal：在现有审查分支修复复审C01–C10，完成真实生产路径回归、干净checkout门禁和同源代码包，提交推送供复审。不重跑R/F，不进入B系列，不操作实验集群。

仓库：zhangzhe-ctrl/ani-installer
工作区：/home/chabking/workspace/ani-installer
分支：review/installer-f-remediation-20260926
资料目录：docs/execution/remediation/20260927/

【恢复与依据】
先读适用AGENTS、现有WORKLOG/progress/status-decision，以及资料目录中的ANI-installer-review-ee8c4cc-20260927.md。复现ZIP只在独立临时目录解压，先读README和相关源码，不覆盖产品文件。
报告固定审查ee8c4ccf4f6168d94c6abbafd13896f2d3af2061；它是定位基线，不是回退目标。核对当前HEAD、远端、工作树和已有改动，保留其后有效修复。不得reset/clean/stash、强推或自动rebase。
报告的函数/调用链是定位依据，行号变化不是阻塞。片段复现不是完整仓库测试；将触发条件迁入现有生产入口回归。若条目已修复，以当前源码和同条件正反测试说明，不重复实现；如有反证，逐项保留供复审，不只凭自述驳回。

【权限和边界】
允许本地及既定Fedora开发环境修改源码、隔离fixture/fake API/子进程测试、联网准备锁定Chart和已有构建依赖、构建代码包；允许提交并推送上述review分支，创建或更新Draft PR。
禁止推main、合并、force push、正式Release、部署及任何三台节点/ESXi操作，包括现场smoke、acceptance、SQL写入、删Pod、快照、重启、抢锁。隔离测试移除真实kubeconfig/SSH凭据输入，不加载实际lab.env。
本轮权限覆盖旧goal中冲突的提交/推送限制；不扩大旧实机授权。密码不改、不输出秘密，已有090732专项额度不重置。保留健康底座和材料备份。

【连续执行四组；C10的材料准备可提前解除门禁前置】
一、C01/C02/C03/C07：目标绑定与取消。
C01：install-success与components-execution两条verify路径共用实际集群身份校验。协议写入/变更前核对原base、当前ClusterUID和目标身份；acceptance限定原installerNode，持共用产品锁后复核关键事实，不另建分布式锁。错集群、非安装主机、错owner或PVC挂载必须零业务写入拒绝，测试到RunVerify。
C02：修正CommandContext已有非空Cancel导致进程组回调未注册。启动前明确注册本任务取消处理，限制收尾时间，只结束本任务进程。测试让已启动后代自己延迟写文件，确认取消后不再活动/写入；不能只检查父bash末行未执行。测试清理自己创建的进程，不留孤儿sleep。
C03：DELETE请求携带原授权Pod UID的服务端前置条件；最后GET后被同名替换必须冲突拒绝，不刷新UID重试删除。核对controller UID/controller标志及Pod实际PVC/PV关系。意图账本补必要文件/目录持久化屏障，失败停止变更，保留base+目标额度。全部用隔离API复现，不做真实PG重建。
C07：统一显式kubeconfig，从Go到子kk、role、Helm/kubectl和checker保持同一上下文；不同层不能各自静默回默认值。用互相冲突的dummy配置、HOME和环境执行真实渲染role/checker，检查最终命令目标，而不只断言父环境变量。

二、C04/C08：真实验证器。
C04：修复runCheckJob的Job层级，优先用已有Kubernetes类型序列化；解析实际发射清单并断言spec.template及containers，fake apply不得无条件放行畸形对象。本地可确定的错误先拒绝，不能先消耗额度。实际调用NATS发布/读回路径测试，不用PG测试代替。
显式请求未实现的专项应在变更前非零拒绝；全skipped/部分必需项未执行不能overall=pass。原范围要求的metrics/fluent-bit重型验证须接正式受控入口，不能删除功能、全部skip、直接手跑脚本绕过授权账本或重复删同一目标来结项；尚未接通就保留未完成。
C08：Fluent Bit client及各node marker用本attempt唯一名，记录UID，仅清理本次拥有的探针；失败先取证，不清历史对象。保留真实采集断言，不加盲目重试。回归两次/并发执行互不删除对方资源，不能只测生成字符串。

三、C05/C06/C09：新增、no-op及记录。
C05：区分完整请求+依赖范围与本次写入范围；持锁复核完整集合，已满足依赖不再跑role。覆盖真实依赖场景：Fluent Bit已安装、OpenSearch待新增。纯no-op同样重查当前owner/revision/version/健康和必要配置；计划后相关变化须拒绝并要求重计划，不把新读UID直接塞进旧声明。
C06：按operation处理配置摘要。合法noop允许与base同摘要，生产writer→loader→smoke可用；不让用户修改无关配置凑摘要差异。仍为didInstall=false，不扩scope、不重开专项额度。
C09：必要成功记录/最终状态落盘完成后才宣告成功，任何写失败影响返回值，不能命令报错而持久状态仍succeeded。保留真实已发生动作，不自动重装。注入成功记录写失败、最终state写失败并测正常成功；测试走RunInstall生产控制流。
随本组把新增role的connections片段/日志传到本次独立目录，不再写入base的connections.d；首装默认行为保留。核对base记录及已有输出前后不变。

四、C10：干净环境门禁。
采用已确定方案：在正式gate之前显式准备现有锁定的6项Chart，按结构化锁中的来源/版本/完整SHA256校验，放到测试声明的位置。联网仅在准备阶段；已有包也验摘要；缓存命中也复核，错误内容不能采用。材料准备逻辑本地与CI复用，不依赖先跑成功gate才能产生下载工具。
测试本体与离线目标不偷偷联网补材料；不升级Chart/镜像、不改批准摘要凑通过、不把大型artifact提交Git，不使用t.Skip、||true、continue-on-error或删除断言绕过。
覆盖无Chart+空缓存的完整准备与门禁，以及缺包/错摘要失败；正式builtin、connector、真实checker和必要Python依赖不得漏跑。工具链按本仓go.mod和现有发布要求核对，记录实际版本，不沿用其他仓库的固定版本要求。

【验收与效率】
每项先命中生产缺陷，再最小修复和正反回归。fake只替换外部系统，不替换被测业务逻辑。测试成功须确认预期用例实际执行、断言命中，零匹配/必需项skip不能通过；负向rc=1必须是预期原因，不能拿缺文件/连接失败冒充拒绝逻辑。
先跑相关测试，接口联动后跑必要完整门禁；最终从已提交候选的独立干净checkout，用相同准备入口跑完整gate并构建代码包，确保没有借用工作树残留。源码未变且身份可核对的门禁可复用，不机械反复全量制包。
受本轮修改直接影响的编译/回归必须修复；无关新发现单列，不扩成无限全仓审计。缺外部材料/认证只阻塞依赖步骤，继续独立代码工作，不因“没有实验锁/实机授权”停本轮代码任务。
每组接着下一组，不只写Next交接。真正额度/工具限制中断时，保存准确断点、任务进程及退出状态，恢复后继续；不能保证的后台工作不要承诺。

【提交、推送和结束】
沿用原台账，按C01–C10记录问题→修改文件→实际测试→结果；区分code_fixed/local_gate_pass/ci_pass/live_not_run，不改写历史日志。
提交本轮明确归属的代码、测试、workflow和小型脱敏证据，逐路径核对，不git add全仓。推同一review分支，读取远端完整SHA确认成功；不上传口令/kubeconfig/lab配置、大包或材料store。
只用现有正常Git认证；失败就保留提交、交完整SHA与精确push命令供用户手动推，不扫描凭据库、不索要聊天中明文Token，也不宣称已推送。
对应当前候选读取实际CI；代码/准备步骤失败就定点修复，无新改变不反复重跑。CI排队/平台故障如实pending/blocked，不伪造绿色，也不阻止交出候选SHA。
结束条件：C01–C10逐项有实现/测试或可执行反证，必要本地门禁与代码包完成，候选已提交且推送结果可核对，CI状态真实。未满足项明确列出，不宣称全部完成。
原add实机、持续离线冷启动及新代码的现场验收本轮保持待测，不拿旧live记录替代、不据此回退重装。最后交分支、完整SHA、C01–C10结果表、测试/代码包标识、CI链接/结论和剩余缺口后停止，等待代码复审，不自动合并或进入B系列。
