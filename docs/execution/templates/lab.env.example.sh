# 示例，不含真实凭据；不是installer配置。复制到仓库外再填写。
# LOCAL变量与Fedora/TARGET变量含义不同，不能把同名路径盲目沿用到另一台机。
export REPO_LOCAL='/REPLACE_WITH_LOCAL_REPO_ROOT'
export PLAN_DIR='/REPLACE_WITH_THIS_PLAN_DIRECTORY'
export EVIDENCE='/REPLACE_WITH_PRIVATE_EVIDENCE_OUTSIDE_REPO'
# LOCAL传输时填写Fedora上的绝对路径
export FEDORA_SRC='/REPLACE_WITH_FEDORA_REPO_ROOT'
# FEDORA会话中使用
export GO_BIN='/REPLACE_WITH_APPROVED_GO_BINARY'
export CODE_OUT='/REPLACE_WITH_NEW_FEDORA_CODE_RELEASE'
export ARTIFACT_OUT='/REPLACE_WITH_NEW_FEDORA_ARTIFACT_RELEASE'
export LAB_LOCK_DIR='/REPLACE_WITH_PRIVATE_LAB_LOCK_DIRECTORY'
# TARGET会话中单独设置，必须已经传输/核对
export CODE_ROOT='/REPLACE_WITH_TARGET_CODE_RELEASE'
export SITE_FILE='/REPLACE_WITH_TARGET_PRIVATE_SITE_YAML'
export ARTIFACT_DIR='/REPLACE_WITH_TARGET_VERIFIED_ARTIFACT'
export RUN_JSON='/REPLACE_WITH_ACTUAL_RUN_JSON'
export CHECKS_DIR='/REPLACE_WITH_CODE_RELEASE/checks'
# 不在此文件中保存密码。现有凭据file只按权限访问，不cat、不进入Git。
